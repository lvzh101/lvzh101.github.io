<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>数学零基础</title>
    <link href="/2026/04/29/%E6%95%B0%E5%AD%A6%E9%9B%B6%E5%9F%BA%E7%A1%80/"/>
    <url>/2026/04/29/%E6%95%B0%E5%AD%A6%E9%9B%B6%E5%9F%BA%E7%A1%80/</url>
    
    <content type="html"><![CDATA[<p>这篇文章用来长期整理：</p><ul><li>题目</li><li>做题技巧</li><li>容易反复遇到的方法</li></ul><p>后面可以继续按同样格式往下追加，不用每次重新想排版。</p><hr><h2 id="技巧整理"><a href="#技巧整理" class="headerlink" title="技巧整理"></a>技巧整理</h2><h3 id="1-看到-f-x-x-，优先转成-f-x-x-0"><a href="#1-看到-f-x-x-，优先转成-f-x-x-0" class="headerlink" title="1. 看到 $f(x)&#x3D;x$，优先转成 $f(x)-x&#x3D;0$"></a>1. 看到 $f(x)&#x3D;x$，优先转成 $f(x)-x&#x3D;0$</h3><p>如果题目里反复出现函数值和自变量的比较，优先考虑作差：</p><p>$$<br>f(x)-x<br>$$</p><p>这类转化通常更容易利用根、符号和区间信息。</p><h3 id="2-已知二次式有两个根时，优先考虑因式分解"><a href="#2-已知二次式有两个根时，优先考虑因式分解" class="headerlink" title="2. 已知二次式有两个根时，优先考虑因式分解"></a>2. 已知二次式有两个根时，优先考虑因式分解</h3><p>如果已知两个根是 $x_1,x_2$，就优先往下面这个形式去想：</p><p>$$<br>a(x-x_1)(x-x_2)<br>$$</p><p>这样更方便直接判断正负、区间关系和大小关系。</p><h3 id="3-遇到双边不等式，拆成两个单边不等式"><a href="#3-遇到双边不等式，拆成两个单边不等式" class="headerlink" title="3. 遇到双边不等式，拆成两个单边不等式"></a>3. 遇到双边不等式，拆成两个单边不等式</h3><p>比如要证明</p><p>$$<br>x&lt;f(x)&lt;x_1<br>$$</p><p>就可以拆成：</p><ul><li>证明 $f(x)-x&gt;0$</li><li>证明 $f(x)-x_1&lt;0$</li></ul><p>这样思路会更清楚。</p><h3 id="4-涉及二次函数对称轴，先写出公式"><a href="#4-涉及二次函数对称轴，先写出公式" class="headerlink" title="4. 涉及二次函数对称轴，先写出公式"></a>4. 涉及二次函数对称轴，先写出公式</h3><p>二次函数</p><p>$$<br>f(x)&#x3D;ax^2+bx+c<br>$$</p><p>的对称轴是</p><p>$$<br>x&#x3D;-\frac{b}{2a}<br>$$</p><p>遇到这类题，先把公式写出来，再想办法把参数用已知条件表示。</p><h3 id="5-题目中的额外条件，往往就是关键条件"><a href="#5-题目中的额外条件，往往就是关键条件" class="headerlink" title="5. 题目中的额外条件，往往就是关键条件"></a>5. 题目中的额外条件，往往就是关键条件</h3><p>像</p><p>$$<br>x_2&lt;\frac{1}{a}<br>$$</p><p>这种看起来比较特别的条件，通常不是摆设。做题时要主动想：它到底服务于哪一步。</p><hr><h2 id="题目整理"><a href="#题目整理" class="headerlink" title="题目整理"></a>题目整理</h2><h2 id="题目-01：二次函数与方程-f-x-x"><a href="#题目-01：二次函数与方程-f-x-x" class="headerlink" title="题目 01：二次函数与方程 $f(x)&#x3D;x$"></a>题目 01：二次函数与方程 $f(x)&#x3D;x$</h2><h3 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h3><p>设二次函数</p><p>$$<br>f(x)&#x3D;ax^2+bx+c \quad (a&gt;0)<br>$$</p><p>方程</p><p>$$<br>f(x)&#x3D;x<br>$$</p><p>有两个实根 $x_1,x_2$，且满足</p><p>$$<br>0&lt;x_1&lt;x_2&lt;\frac{1}{a}<br>$$</p><ol><li>当 $x\in(0,x_1)$ 时，证明：$x&lt;f(x)&lt;x_1$</li><li>设函数 $f(x)$ 的图像关于直线 $x&#x3D;x_0$ 对称，证明：</li></ol><p>$$<br>x_0&lt;\frac{x_1}{2}<br>$$</p><h3 id="这题对应的技巧"><a href="#这题对应的技巧" class="headerlink" title="这题对应的技巧"></a>这题对应的技巧</h3><ul><li>把 $f(x)&#x3D;x$ 转成 $f(x)-x&#x3D;0$</li><li>利用已知根把式子写成因式分解形式</li><li>用作差法处理双边不等式</li><li>用对称轴公式处理参数关系</li><li>注意使用条件 $x_2&lt;\dfrac{1}{a}$</li></ul><hr><h2 id="后续追加格式"><a href="#后续追加格式" class="headerlink" title="后续追加格式"></a>后续追加格式</h2><p>后面继续补题时，直接复制下面这个结构即可。</p><h3 id="题目-02：题目名称"><a href="#题目-02：题目名称" class="headerlink" title="题目 02：题目名称"></a>题目 02：题目名称</h3><h4 id="题目-1"><a href="#题目-1" class="headerlink" title="题目"></a>题目</h4><p>在这里写题目。</p><h4 id="这题对应的技巧-1"><a href="#这题对应的技巧-1" class="headerlink" title="这题对应的技巧"></a>这题对应的技巧</h4><ul><li>技巧 1</li><li>技巧 2</li><li>技巧 3</li></ul><hr><h3 id="题目-03：题目名称"><a href="#题目-03：题目名称" class="headerlink" title="题目 03：题目名称"></a>题目 03：题目名称</h3><h4 id="题目-2"><a href="#题目-2" class="headerlink" title="题目"></a>题目</h4><p>在这里写题目。</p><h4 id="这题对应的技巧-2"><a href="#这题对应的技巧-2" class="headerlink" title="这题对应的技巧"></a>这题对应的技巧</h4><ul><li>技巧 1</li><li>技巧 2</li><li>技巧 3</li></ul>]]></content>
    
    
    <categories>
      
      <category>数学</category>
      
    </categories>
    
    
    <tags>
      
      <tag>数学</tag>
      
      <tag>解题技巧</tag>
      
      <tag>二次函数</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hexo 进阶使用与配置教程</title>
    <link href="/2026/03/10/Hexo%E6%95%99%E7%A8%8B/"/>
    <url>/2026/03/10/Hexo%E6%95%99%E7%A8%8B/</url>
    
    <content type="html"><![CDATA[<p>Hexo 是一个快速、简洁且高效的博客框架，基于 Node.js 实现。它能将 Markdown 文件极速渲染成静态网页，并且搭配丰富的主题和插件，是开发者和写作爱好者的首选工具之一。</p><p>在使用 Hexo 进行博客的日常写作与维护时，除了掌握基础命令，了解如何配置主题和管理文章分类也非常重要。下面为大家整理了一份 <strong>Hexo 进阶使用指南</strong>。</p><h2 id="一、基础命令：写作与本地调试"><a href="#一、基础命令：写作与本地调试" class="headerlink" title="一、基础命令：写作与本地调试"></a>一、基础命令：写作与本地调试</h2><h3 id="1-初始化博客"><a href="#1-初始化博客" class="headerlink" title="1. 初始化博客"></a>1. 初始化博客</h3><p>如果你是第一次使用 Hexo 搭建博客，你需要在空文件夹下执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo init [folder]<br><span class="hljs-built_in">cd</span> [folder]<br>npm install<br></code></pre></td></tr></table></figure><p>这会在你指定的文件夹（或者当前文件夹）下初始化一套 Hexo 博客的源码和必要依赖项。</p><h3 id="2-创建新文章-页面"><a href="#2-创建新文章-页面" class="headerlink" title="2. 创建新文章&#x2F;页面"></a>2. 创建新文章&#x2F;页面</h3><p>平时写作使用最多的命令！</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 新建一篇文章，默认生成在 source/_posts 目录下</span><br>hexo new [layout] <span class="hljs-string">&quot;文章名称&quot;</span><br><br><span class="hljs-comment"># 简写用法，默认 layout 为 post</span><br>hexo n <span class="hljs-string">&quot;我的第一篇博客&quot;</span><br></code></pre></td></tr></table></figure><p>你可以将 <code>layout</code> 替换为：</p><ul><li><code>post</code>：普通文章（默认）</li><li><code>page</code>：独立页面（比如“关于我”、“友链”这类单页）</li><li><code>draft</code>：草稿。会被保存在 <code>source/_drafts</code>，默认不会被编译到线上。</li></ul><p>如果你写了一篇草稿觉得写完了，可以通过下面命令它把它转正发布：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo publish <span class="hljs-string">&quot;草稿名称&quot;</span><br></code></pre></td></tr></table></figure><h3 id="3-生成静态文件"><a href="#3-生成静态文件" class="headerlink" title="3. 生成静态文件"></a>3. 生成静态文件</h3><p> Hexo 的核心就是把 <code>.md</code> 变成 <code>.html</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo generate<br><br><span class="hljs-comment"># 简写用法</span><br>hexo g<br></code></pre></td></tr></table></figure><p>执行后，所有的文件将被渲染并保存在 <code>public</code> 目录下。</p><h3 id="4-启动本地服务器"><a href="#4-启动本地服务器" class="headerlink" title="4. 启动本地服务器"></a>4. 启动本地服务器</h3><p>写完文章想看看效果？在本地起个服务器预览：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo server<br><br><span class="hljs-comment"># 简写用法</span><br>hexo s<br></code></pre></td></tr></table></figure><p>默认情况下，访问网址为：<code>http://localhost:4000/</code>。你只要按 <code>Ctrl + C</code> 就能关闭这个服务。</p><blockquote><p><strong>提示</strong>：目前较新的 Hexo 版本中，<code>hexo s</code> 支持“热更新”，也就是说在启动服务期间你修改 Markdown 保存后，直接在浏览器刷新就能看到效果，无需重新执行 <code>hexo g</code>。</p></blockquote><h3 id="5-清除缓存"><a href="#5-清除缓存" class="headerlink" title="5. 清除缓存"></a>5. 清除缓存</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean<br></code></pre></td></tr></table></figure><p>如果你修改了配置文件（如 <code>_config.yml</code>）或者更换了主题，发现重新渲染后页面并没有生效，通常是因为之前生成的 <code>.html</code> 文件或者缓存作祟。这时使用 <strong><code>hexo clean</code></strong> 清除本地的数据库和生成的静态文件（也就是删掉 <code>public</code> 文件夹）通常都能“包治百病”。</p><hr><h2 id="二、部署相关"><a href="#二、部署相关" class="headerlink" title="二、部署相关"></a>二、部署相关</h2><p>写完文章并在本地满意之后，我们需要把它部署到静态网站托管服务（如 GitHub Pages、Vercel 等）。</p><h3 id="一键部署"><a href="#一键部署" class="headerlink" title="一键部署"></a>一键部署</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo deploy<br><br><span class="hljs-comment"># 简写用法</span><br>hexo d<br></code></pre></td></tr></table></figure><p>这个命令会根据你站点的根目录下的 <code>_config.yml</code> 中配置的 <code>deploy</code> 参数，自动帮你把 <code>public</code> 文件夹下的静态文件推送到远程服务器。</p><h3 id="常用组合技（强烈推荐）"><a href="#常用组合技（强烈推荐）" class="headerlink" title="常用组合技（强烈推荐）"></a>常用组合技（强烈推荐）</h3><p>我们在日常发博客时，往往是写完后：清缓存 -&gt; 重新生成 -&gt; 部署上云，所以为了省事，推荐使用以下简写命令连招：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 清除缓存并重新生成并在本地预览</span><br>hexo clean &amp;&amp; hexo g &amp;&amp; hexo s<br><br><span class="hljs-comment"># 清除缓存、生成静态文件并一键部署</span><br>hexo clean &amp;&amp; hexo g -d<br></code></pre></td></tr></table></figure><p>这里的 <code>-d</code> 其实就是 <code>--deploy</code>，它会让 Hexo 在成功生成文件后，立即执行部署动作，非常丝滑。</p><hr><h2 id="三、手动创建文章与-Front-Matter"><a href="#三、手动创建文章与-Front-Matter" class="headerlink" title="三、手动创建文章与 Front Matter"></a>三、手动创建文章与 Front Matter</h2><p>除了使用 <code>hexo n</code>，你也可以直接手动在 <code>source/_posts/</code> 目录下创建 <code>.md</code> 文件。</p><h3 id="1-Front-Matter-结构"><a href="#1-Front-Matter-结构" class="headerlink" title="1. Front Matter 结构"></a>1. Front Matter 结构</h3><p>手动创建时，必须在文件开头添加 YAML 格式的配置信息：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs markdown">---<br>title: 文章标题<br>date: 2026-03-10 17:00:00<br>tags:<br><span class="hljs-bullet">  -</span> 标签1<br><span class="hljs-bullet">  -</span> 标签2<br>categories:<br><span class="hljs-section">  - 分类</span><br><span class="hljs-section">---</span><br></code></pre></td></tr></table></figure><h3 id="2-注意事项"><a href="#2-注意事项" class="headerlink" title="2. 注意事项"></a>2. 注意事项</h3><ul><li>文件编码必须是 <code>UTF-8</code>。</li><li><code>title</code> 建议与文件名一致，但也可以不同。</li><li>如果手动添加了文件，建议运行 <code>hexo clean</code> 确保 Hexo 能正确扫描到新文件。</li></ul><hr><h2 id="四、主题配置（以-Fluid-为例）"><a href="#四、主题配置（以-Fluid-为例）" class="headerlink" title="四、主题配置（以 Fluid 为例）"></a>四、主题配置（以 Fluid 为例）</h2><p>如果你使用了 Fluid 主题，许多界面设置都在 <code>_config.fluid.yml</code> 中。</p><h3 id="1-文章大纲（TOC）位置"><a href="#1-文章大纲（TOC）位置" class="headerlink" title="1. 文章大纲（TOC）位置"></a>1. 文章大纲（TOC）位置</h3><p>如果你想修改文章右侧&#x2F;左侧的大纲位置，可以修改：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">post:</span><br>  <span class="hljs-attr">toc:</span><br>    <span class="hljs-attr">placement:</span> <span class="hljs-string">right</span>  <span class="hljs-comment"># 可选 left 或 right</span><br></code></pre></td></tr></table></figure><hr><h2 id="五、文章分类（Categories）详解"><a href="#五、文章分类（Categories）详解" class="headerlink" title="五、文章分类（Categories）详解"></a>五、文章分类（Categories）详解</h2><p>分类在 <code>Front Matter</code> 中配置，支持多种层级：</p><h3 id="1-基础分类"><a href="#1-基础分类" class="headerlink" title="1. 基础分类"></a>1. 基础分类</h3><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">categories:</span> <span class="hljs-string">技术笔记</span><br></code></pre></td></tr></table></figure><h3 id="2-多级分类（父子关系）"><a href="#2-多级分类（父子关系）" class="headerlink" title="2. 多级分类（父子关系）"></a>2. 多级分类（父子关系）</h3><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">categories:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">后端</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">网络编程</span><br></code></pre></td></tr></table></figure><blockquote><p><strong>效果</strong>：后端 &gt; 网络编程</p></blockquote><h3 id="3-同时属于多个分类"><a href="#3-同时属于多个分类" class="headerlink" title="3. 同时属于多个分类"></a>3. 同时属于多个分类</h3><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">categories:</span><br>  <span class="hljs-bullet">-</span> [<span class="hljs-string">Linux</span>]<br>  <span class="hljs-bullet">-</span> [<span class="hljs-string">面试题</span>]<br></code></pre></td></tr></table></figure><blockquote><p><strong>效果</strong>：文章会同时出现在 Linux 和 面试题 两个分类下，且两者是并列关系。</p></blockquote><hr><h2 id="六、常用进阶命令"><a href="#六、常用进阶命令" class="headerlink" title="六、常用进阶命令"></a>六、常用进阶命令</h2><h3 id="查看-Hexo-及相关依赖的版本"><a href="#查看-Hexo-及相关依赖的版本" class="headerlink" title="查看 Hexo 及相关依赖的版本"></a>查看 Hexo 及相关依赖的版本</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo version<br></code></pre></td></tr></table></figure><h3 id="列出网站的所有文章、页面、分类和标签数据"><a href="#列出网站的所有文章、页面、分类和标签数据" class="headerlink" title="列出网站的所有文章、页面、分类和标签数据"></a>列出网站的所有文章、页面、分类和标签数据</h3><p>用于有时候查找路由、调试分类数据等。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo list &lt;<span class="hljs-built_in">type</span>&gt; <br><span class="hljs-comment"># 例如：hexo list post / hexo list category / hexo list tag</span><br></code></pre></td></tr></table></figure><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>掌握了以上技巧，你就不仅能用 Hexo 写简单的文字，还能灵活地管理你的博客内容和界面布局。只要熟练使用：<strong>写文章 -&gt; 调配置 -&gt; 编译 -&gt; 部署</strong> 的闭环，你的博客就会越来越专业！</p>]]></content>
    
    
    <categories>
      
      <category>经验分享</category>
      
      <category>博客教程</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Hexo</tag>
      
      <tag>博客教程</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Windows与Linux本地Nginx代理配置指南</title>
    <link href="/2026/03/09/Windows%E4%B8%8ELinux%E6%9C%AC%E5%9C%B0Nginx%E4%BB%A3%E7%90%86%E9%85%8D%E7%BD%AE%E6%8C%87%E5%8D%97/"/>
    <url>/2026/03/09/Windows%E4%B8%8ELinux%E6%9C%AC%E5%9C%B0Nginx%E4%BB%A3%E7%90%86%E9%85%8D%E7%BD%AE%E6%8C%87%E5%8D%97/</url>
    
    <content type="html"><![CDATA[<p>这篇文章整理了一个<strong>本机通过 Nginx 接管 <code>api.openai.com</code> 请求</strong>的完整方案，覆盖 <code>Windows</code> 和 <code>Linux</code> 两种环境，并总结了常见报错、排查方法和长期稳定运行建议。</p><p>这套方案的核心思路是：</p><ul><li>通过 <code>hosts</code> 把 <code>api.openai.com</code> 解析到本机</li><li>由本机 <code>Nginx</code> 接收 <code>443</code> 请求</li><li>再由 <code>Nginx</code> 转发到上游服务，例如 <code>https://right.codes/codex/</code></li></ul><p><strong>先说明定位：</strong></p><ul><li>这套方案更适合<strong>本机调试、学习、内网实验</strong></li><li><strong>不推荐</strong>作为公网正式生产方案</li><li>如果追求长期稳定，最佳实践依然是：<strong>自己的域名 + 正规 DNS + 正规证书 + Linux Nginx</strong></li></ul><hr><h2 id="一、整体原理"><a href="#一、整体原理" class="headerlink" title="一、整体原理"></a>一、整体原理</h2><p>以 Windows 为例，链路是这样的：</p><ol><li>程序或浏览器访问 <code>https://api.openai.com/v1/...</code></li><li>Windows 的 <code>hosts</code> 把 <code>api.openai.com</code> 指向 <code>127.0.0.1</code></li><li>本机 <code>Nginx</code> 监听 <code>443</code></li><li><code>Nginx</code> 把请求转发到上游，例如 <code>https://right.codes/codex/v1/...</code></li></ol><p>Linux 也是同样的原理，只不过：</p><ul><li>Windows 改的是 <code>C:\Windows\System32\drivers\etc\hosts</code></li><li>Linux 改的是 <code>/etc/hosts</code></li></ul><hr><h2 id="二、Windows-配置方法"><a href="#二、Windows-配置方法" class="headerlink" title="二、Windows 配置方法"></a>二、Windows 配置方法</h2><h3 id="1-修改-hosts"><a href="#1-修改-hosts" class="headerlink" title="1. 修改 hosts"></a>1. 修改 hosts</h3><p>编辑文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">C:\Windows\System32\drivers\etc\hosts<br></code></pre></td></tr></table></figure><p>建议只保留这一行映射：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">127.0.0.1 api.openai.com<br></code></pre></td></tr></table></figure><p><strong>不建议加这一行：</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">::1 api.openai.com<br></code></pre></td></tr></table></figure><p>原因：</p><ul><li>实测 <code>curl</code> 往往走 IPv4 没问题</li><li><code>Edge</code>&#x2F;部分浏览器可能优先走 IPv6</li><li>Windows + 浏览器 + <code>::1</code> 更容易出现连接异常、缓存异常、行为不一致的问题</li></ul><p>因此这套方案最稳妥的做法是：<strong>强制走 IPv4</strong>。</p><hr><h3 id="2-准备证书"><a href="#2-准备证书" class="headerlink" title="2. 准备证书"></a>2. 准备证书</h3><p>需要至少有这两个文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">E:\nginx-1.15.5\cert\api.openai.com.crt<br>E:\nginx-1.15.5\cert\api.openai.com.key<br></code></pre></td></tr></table></figure><p>如果只是本机测试，可以使用自签名证书；如果浏览器要无警告访问，则还需要把对应根证书导入 Windows 的“受信任的根证书颁发机构”。</p><hr><h3 id="3-Nginx-完整配置文件"><a href="#3-Nginx-完整配置文件" class="headerlink" title="3. Nginx 完整配置文件"></a>3. Nginx 完整配置文件</h3><p>将下面内容保存为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">E:\nginx-1.15.5\conf\nginx.conf<br></code></pre></td></tr></table></figure><p>完整配置如下：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">worker_processes</span>  <span class="hljs-number">1</span>;<br><br><span class="hljs-attribute">error_log</span>  logs/<span class="hljs-literal">error</span>.log <span class="hljs-literal">notice</span>;<br><br><span class="hljs-section">events</span> &#123;<br>    <span class="hljs-attribute">worker_connections</span>  <span class="hljs-number">1024</span>;<br>&#125;<br><br><span class="hljs-section">http</span> &#123;<br>    <span class="hljs-attribute">include</span>       mime.types;<br>    <span class="hljs-attribute">default_type</span>  application/octet-stream;<br><br>    <span class="hljs-attribute">access_log</span>    logs/access.log;<br>    <span class="hljs-attribute">sendfile</span>      <span class="hljs-literal">on</span>;<br>    <span class="hljs-attribute">keepalive_timeout</span>  <span class="hljs-number">65</span>;<br><br>    <span class="hljs-section">server</span> &#123;<br>        <span class="hljs-attribute">listen</span>       <span class="hljs-number">80</span>;<br>        <span class="hljs-attribute">server_name</span>  api.openai.com;<br><br>        <span class="hljs-attribute">return</span> <span class="hljs-number">301</span> https://<span class="hljs-variable">$host</span><span class="hljs-variable">$request_uri</span>;<br>    &#125;<br><br>    <span class="hljs-section">server</span> &#123;<br>        <span class="hljs-attribute">listen</span>       <span class="hljs-number">443</span> ssl;<br>        <span class="hljs-attribute">server_name</span>  api.openai.com;<br><br>        <span class="hljs-attribute">ssl_certificate</span>      ../cert/api.openai.com.crt;<br>        <span class="hljs-attribute">ssl_certificate_key</span>  ../cert/api.openai.com.key;<br><br>        <span class="hljs-attribute">ssl_session_cache</span>    shared:SSL:<span class="hljs-number">10m</span>;<br>        <span class="hljs-attribute">ssl_session_timeout</span>  <span class="hljs-number">10m</span>;<br><br>        <span class="hljs-section">location</span> = / &#123;<br>            <span class="hljs-attribute">default_type</span> application/json;<br>            <span class="hljs-attribute">return</span> <span class="hljs-number">200</span> <span class="hljs-string">&quot;&#123;\&quot;status\&quot;:\&quot;ok\&quot;,\&quot;message\&quot;:\&quot;nginx proxy is running\&quot;&#125;&quot;</span>;<br>        &#125;<br><br>        <span class="hljs-section">location</span> = /healthz &#123;<br>            <span class="hljs-attribute">default_type</span> text/plain;<br>            <span class="hljs-attribute">return</span> <span class="hljs-number">200</span> <span class="hljs-string">&quot;ok&quot;</span>;<br>        &#125;<br><br>        <span class="hljs-section">location</span> = /favicon.ico &#123;<br>            <span class="hljs-attribute">return</span> <span class="hljs-number">204</span>;<br>        &#125;<br><br>        <span class="hljs-section">location</span> / &#123;<br>            <span class="hljs-attribute">proxy_pass</span> https://right.codes/codex/;<br>            <span class="hljs-attribute">proxy_http_version</span> <span class="hljs-number">1</span>.<span class="hljs-number">1</span>;<br><br>            <span class="hljs-attribute">proxy_ssl_server_name</span> <span class="hljs-literal">on</span>;<br>            <span class="hljs-attribute">proxy_ssl_name</span> right.codes;<br><br>            <span class="hljs-attribute">proxy_set_header</span> Host right.codes;<br>            <span class="hljs-attribute">proxy_set_header</span> Authorization <span class="hljs-variable">$http_authorization</span>;<br>            <span class="hljs-attribute">proxy_set_header</span> Content-Type <span class="hljs-variable">$http_content_type</span>;<br>            <span class="hljs-attribute">proxy_set_header</span> X-Forwarded-For <span class="hljs-variable">$proxy_add_x_forwarded_for</span>;<br>            <span class="hljs-attribute">proxy_set_header</span> X-Forwarded-Proto <span class="hljs-variable">$scheme</span>;<br>            <span class="hljs-attribute">proxy_set_header</span> X-Real-IP <span class="hljs-variable">$remote_addr</span>;<br><br>            <span class="hljs-attribute">proxy_buffering</span> <span class="hljs-literal">off</span>;<br>            <span class="hljs-attribute">proxy_request_buffering</span> <span class="hljs-literal">off</span>;<br><br>            <span class="hljs-attribute">proxy_connect_timeout</span> <span class="hljs-number">30s</span>;<br>            <span class="hljs-attribute">proxy_send_timeout</span> <span class="hljs-number">300s</span>;<br>            <span class="hljs-attribute">proxy_read_timeout</span> <span class="hljs-number">300s</span>;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="4-这份配置为什么这样写"><a href="#4-这份配置为什么这样写" class="headerlink" title="4. 这份配置为什么这样写"></a>4. 这份配置为什么这样写</h3><h4 id="location"><a href="#location" class="headerlink" title="location = /"></a><code>location = /</code></h4><p>访问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">https://api.openai.com/<br></code></pre></td></tr></table></figure><p>会直接返回：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><span class="hljs-attr">&quot;status&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;ok&quot;</span><span class="hljs-punctuation">,</span><span class="hljs-attr">&quot;message&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;nginx proxy is running&quot;</span><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure><p>这样做的好处是：</p><ul><li>浏览器访问首页时不会再被转发到上游</li><li>可以快速判断本机 Nginx 是否正常</li></ul><h4 id="location-healthz"><a href="#location-healthz" class="headerlink" title="location = /healthz"></a><code>location = /healthz</code></h4><p>访问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">https://api.openai.com/healthz<br></code></pre></td></tr></table></figure><p>返回：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">ok<br></code></pre></td></tr></table></figure><p>这是专门用于健康检查的。</p><h4 id="location-1"><a href="#location-1" class="headerlink" title="location /"></a><code>location /</code></h4><p>这一段是真正的代理逻辑：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">proxy_pass</span> https://right.codes/codex/;<br></code></pre></td></tr></table></figure><p>注意末尾这个 <code>/</code> 非常关键。</p><p>比如本机访问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">/v1/models<br></code></pre></td></tr></table></figure><p>上游会收到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">/codex/v1/models<br></code></pre></td></tr></table></figure><p>如果少了末尾 <code>/</code>，就很容易拼成错误路径：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">/codexv1/models<br></code></pre></td></tr></table></figure><p>这正是之前排查中遇到过的典型问题。</p><h4 id="proxy-ssl-server-name-on"><a href="#proxy-ssl-server-name-on" class="headerlink" title="proxy_ssl_server_name on;"></a><code>proxy_ssl_server_name on;</code></h4><p>这行用来开启 <code>SNI</code>。如果上游走了 <code>Cloudflare</code> 或类似 CDN，这行通常是必须的，否则很容易出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">SSL_do_handshake() failed<br></code></pre></td></tr></table></figure><hr><h3 id="5-启动、停止、重载"><a href="#5-启动、停止、重载" class="headerlink" title="5. 启动、停止、重载"></a>5. 启动、停止、重载</h3><p>进入 Nginx 目录：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell"><span class="hljs-built_in">cd</span> E:\nginx<span class="hljs-literal">-1</span>.<span class="hljs-number">15.5</span><br></code></pre></td></tr></table></figure><p>检查配置：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">.\nginx.exe <span class="hljs-literal">-t</span><br></code></pre></td></tr></table></figure><p>启动：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">.\nginx.exe<br></code></pre></td></tr></table></figure><p>重载配置：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">.\nginx.exe <span class="hljs-literal">-s</span> reload<br></code></pre></td></tr></table></figure><p>停止：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">.\nginx.exe <span class="hljs-literal">-s</span> stop<br></code></pre></td></tr></table></figure><p>查看进程：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">tasklist | findstr nginx<br></code></pre></td></tr></table></figure><hr><h3 id="6-Windows-下验证是否成功"><a href="#6-Windows-下验证是否成功" class="headerlink" title="6. Windows 下验证是否成功"></a>6. Windows 下验证是否成功</h3><p>验证健康检查：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">curl.exe <span class="hljs-literal">-k</span> https://api.openai.com/healthz<br></code></pre></td></tr></table></figure><p>预期输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">ok<br></code></pre></td></tr></table></figure><p>验证首页：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">curl.exe <span class="hljs-literal">-k</span> https://api.openai.com/<br></code></pre></td></tr></table></figure><p>预期输出：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><span class="hljs-attr">&quot;status&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;ok&quot;</span><span class="hljs-punctuation">,</span><span class="hljs-attr">&quot;message&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;nginx proxy is running&quot;</span><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure><p>验证 API 路径：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">curl.exe <span class="hljs-literal">-k</span> https://api.openai.com/v1/models<br></code></pre></td></tr></table></figure><p>如果返回：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><span class="hljs-attr">&quot;error&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;缺少 API Key&quot;</span><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure><p>这反而说明：</p><ul><li>本机 <code>hosts</code> 生效了</li><li>本机 <code>Nginx</code> 正常</li><li>上游代理也通了</li></ul><p>只是没有带 <code>Authorization</code> 头。</p><p>带 Key 的测试方式：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs powershell">curl.exe <span class="hljs-literal">-k</span> https://api.openai.com/v1/models ^<br>  <span class="hljs-literal">-H</span> <span class="hljs-string">&quot;Authorization: Bearer 你的APIKey&quot;</span><br></code></pre></td></tr></table></figure><hr><h3 id="7-Edge-浏览器访问异常的排查"><a href="#7-Edge-浏览器访问异常的排查" class="headerlink" title="7. Edge 浏览器访问异常的排查"></a>7. Edge 浏览器访问异常的排查</h3><p>这个问题在实际排查里很常见：</p><ul><li><code>curl</code> 正常</li><li><code>Edge</code> 报 <code>502</code>、<code>ERR_CONNECTION_CLOSED</code> 或无法打开页面</li></ul><p>通常不是 Nginx 坏了，而是 <strong>浏览器没有按和 <code>curl</code> 一样的方式走本机</strong>。</p><p>建议按下面顺序排查：</p><h4 id="第一步：刷新-Windows-DNS"><a href="#第一步：刷新-Windows-DNS" class="headerlink" title="第一步：刷新 Windows DNS"></a>第一步：刷新 Windows DNS</h4><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">ipconfig /flushdns<br></code></pre></td></tr></table></figure><h4 id="第二步：清-Edge-的-DNS-和连接池缓存"><a href="#第二步：清-Edge-的-DNS-和连接池缓存" class="headerlink" title="第二步：清 Edge 的 DNS 和连接池缓存"></a>第二步：清 Edge 的 DNS 和连接池缓存</h4><p>在地址栏打开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">edge://net-internals/#dns<br></code></pre></td></tr></table></figure><p>点击：</p><ul><li><code>Clear host cache</code></li></ul><p>再打开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">edge://net-internals/#sockets<br></code></pre></td></tr></table></figure><p>点击：</p><ul><li><code>Flush socket pools</code></li></ul><h4 id="第三步：关闭-Edge-的安全-DNS"><a href="#第三步：关闭-Edge-的安全-DNS" class="headerlink" title="第三步：关闭 Edge 的安全 DNS"></a>第三步：关闭 Edge 的安全 DNS</h4><p>打开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">edge://settings/privacy<br></code></pre></td></tr></table></figure><p>找到“使用安全 DNS 指定如何查找网站的网络地址”，先关闭。</p><h4 id="第四步：关闭代理-VPN-代理扩展"><a href="#第四步：关闭代理-VPN-代理扩展" class="headerlink" title="第四步：关闭代理&#x2F;VPN&#x2F;代理扩展"></a>第四步：关闭代理&#x2F;VPN&#x2F;代理扩展</h4><p>比如：</p><ul><li>Clash</li><li>Mihomo</li><li>v2rayN</li><li>浏览器代理扩展</li><li>系统代理</li></ul><p>它们都可能导致浏览器不再走本机 <code>hosts</code>。</p><h4 id="第五步：优先测试-healthz"><a href="#第五步：优先测试-healthz" class="headerlink" title="第五步：优先测试 /healthz"></a>第五步：优先测试 <code>/healthz</code></h4><p>如果浏览器访问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">https://api.openai.com/healthz<br></code></pre></td></tr></table></figure><p>都拿不到 <code>ok</code>，那几乎可以确定是浏览器侧网络栈、缓存、代理或 DNS 在干扰。</p><hr><h3 id="8-Windows-开机自启与长期运行建议"><a href="#8-Windows-开机自启与长期运行建议" class="headerlink" title="8. Windows 开机自启与长期运行建议"></a>8. Windows 开机自启与长期运行建议</h3><p>如果只是双击 <code>nginx.exe</code>，那：</p><ul><li>关机后进程会消失</li><li>重启系统后需要重新启动</li></ul><p>因此更推荐把 Nginx 注册为 Windows 服务。</p><p>常见做法：</p><ul><li>使用 <code>NSSM</code> 把 <code>nginx.exe</code> 注册成服务</li><li>或使用“任务计划程序”设置开机启动</li></ul><p>如果希望长期稳定，推荐：</p><ul><li><code>hosts</code> 只保留 IPv4 映射</li><li>关闭浏览器安全 DNS</li><li>避免代理软件接管 <code>api.openai.com</code></li><li>将 <code>nginx.exe</code> 注册成系统服务</li></ul><hr><h2 id="三、Linux-配置方法"><a href="#三、Linux-配置方法" class="headerlink" title="三、Linux 配置方法"></a>三、Linux 配置方法</h2><h3 id="1-Linux-什么时候需要改"><a href="#1-Linux-什么时候需要改" class="headerlink" title="1. Linux 什么时候需要改"></a>1. Linux 什么时候需要改</h3><p>这一点很容易搞混：</p><ul><li><strong>如果真正运行 Nginx 的机器是 Windows</strong>，那 Linux <strong>不需要改</strong></li><li><strong>只有当 Linux 机器自己也要承担代理职责</strong>，Linux 才需要配置</li></ul><p>换句话说：</p><ul><li><code>hosts</code> 是按机器生效的</li><li><code>Nginx</code> 也是在哪台机器运行，就配置哪台机器</li></ul><hr><h3 id="2-安装-Nginx"><a href="#2-安装-Nginx" class="headerlink" title="2. 安装 Nginx"></a>2. 安装 Nginx</h3><p>Ubuntu &#x2F; Debian：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt update<br><span class="hljs-built_in">sudo</span> apt install -y nginx openssl<br></code></pre></td></tr></table></figure><p>CentOS &#x2F; Rocky &#x2F; AlmaLinux：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> dnf install -y nginx openssl<br></code></pre></td></tr></table></figure><p>设置开机自启并立即启动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl <span class="hljs-built_in">enable</span> --now nginx<br></code></pre></td></tr></table></figure><p>查看状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl status nginx --no-pager<br></code></pre></td></tr></table></figure><hr><h3 id="3-修改-Linux-的-hosts"><a href="#3-修改-Linux-的-hosts" class="headerlink" title="3. 修改 Linux 的 hosts"></a>3. 修改 Linux 的 hosts</h3><p>编辑：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">/etc/hosts<br></code></pre></td></tr></table></figure><p>追加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">127.0.0.1 api.openai.com<br></code></pre></td></tr></table></figure><p>编辑命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> nano /etc/hosts<br></code></pre></td></tr></table></figure><p>验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">getent hosts api.openai.com<br></code></pre></td></tr></table></figure><hr><h3 id="4-准备证书"><a href="#4-准备证书" class="headerlink" title="4. 准备证书"></a>4. 准备证书</h3><p>如果只是本机测试，可使用自签名证书。示例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">mkdir</span> -p /etc/nginx/certs<br><span class="hljs-built_in">sudo</span> openssl req -x509 -nodes -days 365 -newkey rsa:2048 \<br>  -keyout /etc/nginx/certs/api.openai.com.key \<br>  -out /etc/nginx/certs/api.openai.com.crt \<br>  -subj <span class="hljs-string">&quot;/CN=api.openai.com&quot;</span><br></code></pre></td></tr></table></figure><p>如果是正式环境，应使用自己域名的正规证书，而不是长期使用 <code>api.openai.com</code> 的本地伪装方案。</p><hr><h3 id="5-Linux-Nginx-配置文件"><a href="#5-Linux-Nginx-配置文件" class="headerlink" title="5. Linux Nginx 配置文件"></a>5. Linux Nginx 配置文件</h3><p>以 Ubuntu &#x2F; Debian 为例，新建：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">/etc/nginx/sites-available/api-openai-local<br></code></pre></td></tr></table></figure><p>配置如下：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-section">server</span> &#123;<br>    <span class="hljs-attribute">listen</span> <span class="hljs-number">80</span>;<br>    <span class="hljs-attribute">server_name</span> api.openai.com;<br><br>    <span class="hljs-attribute">return</span> <span class="hljs-number">301</span> https://<span class="hljs-variable">$host</span><span class="hljs-variable">$request_uri</span>;<br>&#125;<br><br><span class="hljs-section">server</span> &#123;<br>    <span class="hljs-attribute">listen</span> <span class="hljs-number">443</span> ssl http2;<br>    <span class="hljs-attribute">server_name</span> api.openai.com;<br><br>    <span class="hljs-attribute">ssl_certificate</span>     /etc/nginx/certs/api.openai.com.crt;<br>    <span class="hljs-attribute">ssl_certificate_key</span> /etc/nginx/certs/api.openai.com.key;<br><br>    <span class="hljs-attribute">ssl_session_cache</span>   shared:SSL:<span class="hljs-number">10m</span>;<br>    <span class="hljs-attribute">ssl_session_timeout</span> <span class="hljs-number">10m</span>;<br><br>    <span class="hljs-attribute">access_log</span> /var/log/nginx/api.openai.access.log;<br>    <span class="hljs-attribute">error_log</span>  /var/log/nginx/api.openai.<span class="hljs-literal">error</span>.log <span class="hljs-literal">notice</span>;<br><br>    <span class="hljs-section">location</span> = / &#123;<br>        <span class="hljs-attribute">default_type</span> application/json;<br>        <span class="hljs-attribute">return</span> <span class="hljs-number">200</span> <span class="hljs-string">&#x27;&#123;&quot;status&quot;:&quot;ok&quot;,&quot;message&quot;:&quot;nginx proxy is running&quot;&#125;&#x27;</span>;<br>    &#125;<br><br>    <span class="hljs-section">location</span> = /healthz &#123;<br>        <span class="hljs-attribute">default_type</span> text/plain;<br>        <span class="hljs-attribute">return</span> <span class="hljs-number">200</span> <span class="hljs-string">&quot;ok&quot;</span>;<br>    &#125;<br><br>    <span class="hljs-section">location</span> = /favicon.ico &#123;<br>        <span class="hljs-attribute">return</span> <span class="hljs-number">204</span>;<br>    &#125;<br><br>    <span class="hljs-section">location</span> / &#123;<br>        <span class="hljs-attribute">proxy_pass</span> https://right.codes/codex/;<br>        <span class="hljs-attribute">proxy_http_version</span> <span class="hljs-number">1</span>.<span class="hljs-number">1</span>;<br><br>        <span class="hljs-attribute">proxy_ssl_server_name</span> <span class="hljs-literal">on</span>;<br>        <span class="hljs-attribute">proxy_ssl_name</span> right.codes;<br><br>        <span class="hljs-attribute">proxy_set_header</span> Host right.codes;<br>        <span class="hljs-attribute">proxy_set_header</span> Authorization <span class="hljs-variable">$http_authorization</span>;<br>        <span class="hljs-attribute">proxy_set_header</span> Content-Type <span class="hljs-variable">$http_content_type</span>;<br>        <span class="hljs-attribute">proxy_set_header</span> X-Forwarded-For <span class="hljs-variable">$proxy_add_x_forwarded_for</span>;<br>        <span class="hljs-attribute">proxy_set_header</span> X-Forwarded-Proto <span class="hljs-variable">$scheme</span>;<br>        <span class="hljs-attribute">proxy_set_header</span> X-Real-IP <span class="hljs-variable">$remote_addr</span>;<br><br>        <span class="hljs-attribute">proxy_buffering</span> <span class="hljs-literal">off</span>;<br>        <span class="hljs-attribute">proxy_request_buffering</span> <span class="hljs-literal">off</span>;<br><br>        <span class="hljs-attribute">proxy_connect_timeout</span> <span class="hljs-number">30s</span>;<br>        <span class="hljs-attribute">proxy_send_timeout</span> <span class="hljs-number">300s</span>;<br>        <span class="hljs-attribute">proxy_read_timeout</span> <span class="hljs-number">300s</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>启用配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">ln</span> -s /etc/nginx/sites-available/api-openai-local /etc/nginx/sites-enabled/api-openai-local<br><span class="hljs-built_in">sudo</span> nginx -t<br><span class="hljs-built_in">sudo</span> systemctl reload nginx<br></code></pre></td></tr></table></figure><p>如果是 <code>CentOS</code> 系列，更常见的方式是直接写到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">/etc/nginx/conf.d/api-openai-local.conf<br></code></pre></td></tr></table></figure><hr><h3 id="6-Linux-下验证是否成功"><a href="#6-Linux-下验证是否成功" class="headerlink" title="6. Linux 下验证是否成功"></a>6. Linux 下验证是否成功</h3><p>检查健康接口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl -k https://api.openai.com/healthz<br></code></pre></td></tr></table></figure><p>检查首页：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl -k https://api.openai.com/<br></code></pre></td></tr></table></figure><p>检查 API：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">curl -k https://api.openai.com/v1/models<br></code></pre></td></tr></table></figure><p>如果只是返回缺少 API Key，说明代理链路是通的。</p><hr><h3 id="7-Linux-的服务管理"><a href="#7-Linux-的服务管理" class="headerlink" title="7. Linux 的服务管理"></a>7. Linux 的服务管理</h3><p>常用命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl status nginx<br><span class="hljs-built_in">sudo</span> systemctl start nginx<br><span class="hljs-built_in">sudo</span> systemctl stop nginx<br><span class="hljs-built_in">sudo</span> systemctl restart nginx<br><span class="hljs-built_in">sudo</span> systemctl reload nginx<br><span class="hljs-built_in">sudo</span> systemctl <span class="hljs-built_in">enable</span> nginx<br><span class="hljs-built_in">sudo</span> systemctl <span class="hljs-built_in">disable</span> nginx<br></code></pre></td></tr></table></figure><p>查看是否开机自启：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl is-enabled nginx<br></code></pre></td></tr></table></figure><p>查看是否正在运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl is-active nginx<br></code></pre></td></tr></table></figure><hr><h2 id="四、常见问题与解决方法"><a href="#四、常见问题与解决方法" class="headerlink" title="四、常见问题与解决方法"></a>四、常见问题与解决方法</h2><h3 id="1-502-Bad-Gateway"><a href="#1-502-Bad-Gateway" class="headerlink" title="1. 502 Bad Gateway"></a>1. <code>502 Bad Gateway</code></h3><p>这个错误非常常见，但含义要分清：</p><ul><li>浏览器或客户端到本机 Nginx 可能是通的</li><li><strong>真正失败的是 Nginx 到上游</strong></li></ul><p>常见原因：</p><ul><li>上游地址不可达</li><li>上游握手失败</li><li><code>SNI</code> 没开</li><li><code>Host</code> 不匹配</li><li>路径拼错</li></ul><p>重点排查：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell"><span class="hljs-built_in">Get-Content</span> .\logs\error.log <span class="hljs-literal">-Tail</span> <span class="hljs-number">50</span><br></code></pre></td></tr></table></figure><p>或 Linux：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tail</span> -f /var/log/nginx/api.openai.error.log<br></code></pre></td></tr></table></figure><hr><h3 id="2-SSL-do-handshake-failed"><a href="#2-SSL-do-handshake-failed" class="headerlink" title="2. SSL_do_handshake() failed"></a>2. <code>SSL_do_handshake() failed</code></h3><p>典型日志：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">SSL_do_handshake() failed while SSL handshaking to upstream<br></code></pre></td></tr></table></figure><p>通常表示：</p><ul><li>上游是 HTTPS</li><li>但代理没有正确带上 <code>SNI</code></li></ul><p>解决方法：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">proxy_ssl_server_name</span> <span class="hljs-literal">on</span>;<br><span class="hljs-attribute">proxy_ssl_name</span> right.codes;<br><span class="hljs-attribute">proxy_set_header</span> Host right.codes;<br></code></pre></td></tr></table></figure><hr><h3 id="3-路径被拼成-codexv1-models"><a href="#3-路径被拼成-codexv1-models" class="headerlink" title="3. 路径被拼成 /codexv1/models"></a>3. 路径被拼成 <code>/codexv1/models</code></h3><p>典型原因：</p><ul><li><code>proxy_pass</code> 末尾缺少 <code>/</code></li></ul><p>错误写法：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">proxy_pass</span> https://right.codes/codex;<br></code></pre></td></tr></table></figure><p>推荐写法：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">proxy_pass</span> https://right.codes/codex/;<br></code></pre></td></tr></table></figure><hr><h3 id="4-server-directive-is-not-allowed-here"><a href="#4-server-directive-is-not-allowed-here" class="headerlink" title="4. server directive is not allowed here"></a>4. <code>server directive is not allowed here</code></h3><p>典型报错：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">&quot;server&quot; directive is not allowed here<br></code></pre></td></tr></table></figure><p>原因：</p><ul><li>直接把 <code>server {}</code> 写到了 <code>nginx.conf</code> 顶层</li><li>没有放在 <code>http {}</code> 里面</li></ul><p>正确结构应为：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">worker_processes</span> <span class="hljs-number">1</span>;<br><br><span class="hljs-section">events</span> &#123;<br>    <span class="hljs-attribute">worker_connections</span> <span class="hljs-number">1024</span>;<br>&#125;<br><br><span class="hljs-section">http</span> &#123;<br>    <span class="hljs-section">server</span> &#123;<br>        ...<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><hr><h3 id="5-BIO-new-file-crt-failed"><a href="#5-BIO-new-file-crt-failed" class="headerlink" title="5. BIO_new_file(...crt) failed"></a>5. <code>BIO_new_file(...crt) failed</code></h3><p>典型原因：</p><ul><li>证书路径写错</li><li>文件根本不存在</li></ul><p>比如之前排查中曾出现过：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">api.yourdomain.com.crt<br></code></pre></td></tr></table></figure><p>但实际目录里只有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">api.openai.com.crt<br></code></pre></td></tr></table></figure><p>解决方法：</p><ul><li>确认证书路径和实际文件名完全一致</li><li>先执行 <code>nginx -t</code>，确认配置测试通过后再重载</li></ul><hr><h3 id="6-浏览器不通，但-curl-正常"><a href="#6-浏览器不通，但-curl-正常" class="headerlink" title="6. 浏览器不通，但 curl 正常"></a>6. 浏览器不通，但 <code>curl</code> 正常</h3><p>这种现象非常常见。</p><p>常见原因：</p><ul><li>浏览器 DNS 缓存</li><li>浏览器连接池缓存</li><li>代理软件接管了请求</li><li>安全 DNS 绕过了本机 <code>hosts</code></li><li>浏览器扩展改写了网络行为</li></ul><p>解决方法：</p><ul><li>执行 <code>ipconfig /flushdns</code></li><li>清 <code>Edge</code> 的 <code>DNS</code> 和 <code>socket</code> 缓存</li><li>关闭浏览器安全 DNS</li><li>关闭代理&#x2F;VPN&#x2F;代理扩展</li><li>优先访问 <code>/healthz</code> 判断是不是走到了本机 Nginx</li></ul><hr><h3 id="7-浏览器提示-ERR-CONNECTION-CLOSED"><a href="#7-浏览器提示-ERR-CONNECTION-CLOSED" class="headerlink" title="7. 浏览器提示 ERR_CONNECTION_CLOSED"></a>7. 浏览器提示 <code>ERR_CONNECTION_CLOSED</code></h3><p>通常说明：</p><ul><li>本机 Nginx 没成功监听 <code>443</code></li><li>或证书握手被浏览器直接拒绝</li><li>或浏览器走了和 <code>curl</code> 不同的网络链路</li></ul><p>先确认：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs powershell">tasklist | findstr nginx<br>curl.exe <span class="hljs-literal">-k</span> https://api.openai.com/healthz<br></code></pre></td></tr></table></figure><p>如果 <code>curl</code> 成功而浏览器失败，优先排查浏览器侧缓存、代理和 DNS。</p><hr><h3 id="8-重启电脑或重启-Linux-后配置会不会丢"><a href="#8-重启电脑或重启-Linux-后配置会不会丢" class="headerlink" title="8. 重启电脑或重启 Linux 后配置会不会丢"></a>8. 重启电脑或重启 Linux 后配置会不会丢</h3><p>一般不会。</p><p>因为以下内容都保存在磁盘：</p><ul><li><code>hosts</code></li><li><code>nginx.conf</code></li><li>证书文件</li><li>站点配置文件</li></ul><p>但要注意：</p><ul><li><strong>配置不会丢，不代表服务一定会自动启动</strong></li></ul><p>Linux 需要确认：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl is-enabled nginx<br></code></pre></td></tr></table></figure><p>Windows 则建议把 <code>nginx.exe</code> 注册为服务，否则系统重启后需要重新启动。</p><hr><h2 id="五、代理软件场景下的建议"><a href="#五、代理软件场景下的建议" class="headerlink" title="五、代理软件场景下的建议"></a>五、代理软件场景下的建议</h2><p>如果电脑上运行了代理软件，比如：</p><ul><li>Clash</li><li>Clash Verge</li><li>Mihomo</li><li>v2rayN</li><li>其他 TUN &#x2F; 系统代理软件</li></ul><p>最稳的做法不是让代理去代理 <code>api.openai.com</code>，而是：</p><ul><li><strong>让代理软件彻底不要碰这个域名</strong></li></ul><p>建议原则：</p><ul><li><code>api.openai.com</code> 走 <code>DIRECT</code></li><li><code>127.0.0.1</code> 走 <code>DIRECT</code></li><li><code>localhost</code> 走 <code>DIRECT</code></li><li>禁止代理软件用 Fake-IP 或远程 DNS 接管这个域名</li></ul><p>以 Clash&#x2F;Mihomo 为例，可参考：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">hosts:</span><br>  <span class="hljs-attr">&quot;api.openai.com&quot;:</span> <span class="hljs-number">127.0</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span><br><br><span class="hljs-attr">rules:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">DOMAIN,api.openai.com,DIRECT</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">DOMAIN,localhost,DIRECT</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">IP-CIDR,127.0.0.0/8,DIRECT,no-resolve</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">IP-CIDR6,::1/128,DIRECT,no-resolve</span><br><br><span class="hljs-attr">dns:</span><br>  <span class="hljs-attr">use-hosts:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">fake-ip-filter:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">api.openai.com</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">localhost</span><br></code></pre></td></tr></table></figure><p>这套思路的本质是：</p><ul><li><code>api.openai.com</code> 必须交给本机 <code>hosts + Nginx</code> 处理</li><li>不要再被代理软件中途抢走</li></ul><hr><h2 id="六、长期稳定运行建议"><a href="#六、长期稳定运行建议" class="headerlink" title="六、长期稳定运行建议"></a>六、长期稳定运行建议</h2><p>如果希望尽量不再出现“今天能用，明天又不行”，建议遵循下面几点：</p><h3 id="1-固定成-IPv4-only"><a href="#1-固定成-IPv4-only" class="headerlink" title="1. 固定成 IPv4-only"></a>1. 固定成 IPv4-only</h3><p>Windows <code>hosts</code> 只保留：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">127.0.0.1 api.openai.com<br></code></pre></td></tr></table></figure><p>不要额外配 <code>::1</code>。</p><h3 id="2-固定一份最终可用配置"><a href="#2-固定一份最终可用配置" class="headerlink" title="2. 固定一份最终可用配置"></a>2. 固定一份最终可用配置</h3><p>比如备份：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">E:\nginx-1.15.5\conf\nginx.conf.ok<br></code></pre></td></tr></table></figure><p>以后如果又改乱了，可以快速回滚。</p><h3 id="3-每次改配置先执行-nginx-t"><a href="#3-每次改配置先执行-nginx-t" class="headerlink" title="3. 每次改配置先执行 nginx -t"></a>3. 每次改配置先执行 <code>nginx -t</code></h3><p>Windows：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell">.\nginx.exe <span class="hljs-literal">-t</span><br></code></pre></td></tr></table></figure><p>Linux：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> nginx -t<br></code></pre></td></tr></table></figure><h3 id="4-专门保留-healthz"><a href="#4-专门保留-healthz" class="headerlink" title="4. 专门保留 /healthz"></a>4. 专门保留 <code>/healthz</code></h3><p>这能让排查特别快：</p><ul><li><code>/healthz</code> 不通：本机 Nginx 有问题</li><li><code>/healthz</code> 通、API 不通：上游有问题</li></ul><h3 id="5-尽量不要把浏览器首页当成唯一验证方式"><a href="#5-尽量不要把浏览器首页当成唯一验证方式" class="headerlink" title="5. 尽量不要把浏览器首页当成唯一验证方式"></a>5. 尽量不要把浏览器首页当成唯一验证方式</h3><p>更可靠的验证顺序是：</p><ol><li><code>/healthz</code></li><li><code>/</code></li><li><code>/v1/models</code></li><li>带 <code>Authorization</code> 的真实接口请求</li></ol><hr><h2 id="七、最后的建议"><a href="#七、最后的建议" class="headerlink" title="七、最后的建议"></a>七、最后的建议</h2><p>这套“<code>hosts</code> + 本机 Nginx + 反向代理”的方案，优点是：</p><ul><li>上手快</li><li>本机验证方便</li><li>不需要额外买域名</li></ul><p>但它的缺点也很明显：</p><ul><li>容易受到浏览器缓存、代理软件、系统网络栈影响</li><li>长期维护成本高</li><li>不适合正式公网长期使用</li></ul><p>因此建议这样选择：</p><ul><li><strong>只是自己本机调试：</strong> 这套方案可以用</li><li><strong>要长期稳定：</strong> 最好迁移到 Linux，并使用自己的域名</li><li><strong>要正式上线：</strong> 自己域名 + 正规 DNS + 正规证书 + Linux Nginx 才是最佳实践</li></ul><p>如果只是临时测试，这篇文章里的配置已经足够；如果以后要做成长期方案，建议尽早切换到正规部署方式。</p>]]></content>
    
    
    <categories>
      
      <category>配置教程</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Nginx</tag>
      
      <tag>Windows</tag>
      
      <tag>Linux</tag>
      
      <tag>反向代理</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>四表五链</title>
    <link href="/2026/03/06/%E5%9B%9B%E8%A1%A8%E4%BA%94%E9%93%BE/"/>
    <url>/2026/03/06/%E5%9B%9B%E8%A1%A8%E4%BA%94%E9%93%BE/</url>
    
    <content type="html"><![CDATA[<p>iptables 的“<strong>四表五链</strong>”是对 Linux netfilter 防火墙体系的经典概括：</p><ul><li><strong>四表</strong>：raw、mangle、nat、filter</li><li><strong>五链</strong>：PREROUTING、INPUT、FORWARD、OUTPUT、POSTROUTING</li></ul><p>可以理解成：</p><ul><li><strong>表（table）</strong>：做哪一类处理</li><li><strong>链（chain）</strong>：在数据包流转的哪个时机处理</li></ul><hr><h2 id="一、四表是什么"><a href="#一、四表是什么" class="headerlink" title="一、四表是什么"></a>一、四表是什么</h2><h3 id="1-filter-表"><a href="#1-filter-表" class="headerlink" title="1. filter 表"></a>1. filter 表</h3><p>这是最常用的表，主要负责<strong>过滤数据包</strong>。</p><p>常见动作：</p><ul><li>放行：ACCEPT</li><li>丢弃：DROP</li><li>拒绝：REJECT</li></ul><p>典型用途：</p><ul><li>允许 22 端口 SSH</li><li>禁止某个 IP 访问</li><li>限制某类流量进入本机</li></ul><hr><h3 id="2-nat-表"><a href="#2-nat-表" class="headerlink" title="2. nat 表"></a>2. nat 表</h3><p>负责<strong>网络地址转换</strong>。</p><p>常见用途：</p><ul><li>SNAT：源地址转换</li><li>DNAT：目标地址转换</li><li>MASQUERADE：动态伪装，常用于家庭路由器共享上网</li><li>端口映射</li></ul><p>典型场景：</p><ul><li>外网访问内网服务器</li><li>多台内网机器共享一个公网 IP 上网</li></ul><p><strong>回包为什么还能回到真正的内网机器？</strong><br>因为 NAT 网关自己记了映射关系，不是接收方知道的。</p><p>比如网关会维护一张表，大概像这样：</p><p>192.168.1.100:50000 ↔ 1.2.3.4:62001<br>于是流程就是：</p><ol><li><p>内网主机发请求<br>192.168.1.100:50000 -&gt; 8.8.8.8:80</p></li><li><p>NAT 网关改写后发出去<br>1.2.3.4:62001 -&gt; 8.8.8.8:80</p></li><li><p>接收方看到的源<br>接收方只看到：</p></li></ol><p>1.2.3.4:62001</p><p>所以它回包时发给：</p><p>8.8.8.8:80 -&gt; 1.2.3.4:62001</p><ol><li>NAT 网关收到回包<br>网关查自己记录的映射表，发现：</li></ol><p>1.2.3.4:62001 对应的是 192.168.1.100:50000<br>于是再把目标改回去，转给内网主机：</p><p>8.8.8.8:80 -&gt; 192.168.1.100:50000</p><p>所以不是接收方知道原始 IP，<br>而是 NAT 网关靠自己的连接跟踪表，把回包“认领”回去。</p><h4 id="一句话总结"><a href="#一句话总结" class="headerlink" title="一句话总结"></a>一句话总结</h4><ul><li><strong>源 IP 被 NAT 改写后，接收方看到的就是改写后的 IP</strong></li><li><strong>接收方不知道原始源 IP</strong></li><li><strong>真正负责“记住原始发送者是谁”的，是中间的 NAT 网关</strong></li></ul><hr><h3 id="3-mangle-表"><a href="#3-mangle-表" class="headerlink" title="3. mangle 表"></a>3. mangle 表</h3><p>负责<strong>修改数据包内容</strong>，比如一些报文标记、TOS、TTL 等。</p><p>典型用途：</p><ul><li>给数据包打 mark</li><li>配合策略路由使用</li><li>修改某些头部字段</li></ul><p>这个表更偏高级玩法，普通防火墙规则里用得没 filter 和 nat 多。</p><hr><h3 id="4-raw-表"><a href="#4-raw-表" class="headerlink" title="4. raw 表"></a>4. raw 表</h3><p>负责<strong>连接跟踪前的特殊处理</strong>。</p><p>最常见用途：</p><ul><li>配置数据包<strong>不进入连接跟踪</strong>（NOTRACK）</li></ul><p>它优先级很高，但日常使用相对少。</p><hr><h2 id="二、五链是什么"><a href="#二、五链是什么" class="headerlink" title="二、五链是什么"></a>二、五链是什么</h2><p>链表示数据包在内核网络协议栈里流动时，会经过的几个关键位置。</p><h3 id="1-PREROUTING"><a href="#1-PREROUTING" class="headerlink" title="1. PREROUTING"></a>1. PREROUTING</h3><p>数据包<strong>刚进入网卡，还没决定路由前</strong>。</p><p>适合做：</p><ul><li>DNAT</li><li>提前修改包</li><li>某些标记操作</li></ul><hr><h3 id="2-INPUT"><a href="#2-INPUT" class="headerlink" title="2. INPUT"></a>2. INPUT</h3><p>数据包的目标就是<strong>本机</strong>。</p><p>比如：</p><ul><li>别人 SSH 到你的服务器</li><li>浏览器访问你机器上的 Web 服务</li></ul><p>适合做：</p><ul><li>控制哪些流量能进入本机</li></ul><hr><h3 id="3-FORWARD"><a href="#3-FORWARD" class="headerlink" title="3. FORWARD"></a>3. FORWARD</h3><p>数据包<strong>不是发给本机，而是经过本机转发</strong>。</p><p>比如：</p><ul><li>你的 Linux 机器充当路由器</li><li>一台机器经过这台服务器转发到另一台</li></ul><p>适合做：</p><ul><li>控制转发流量是否允许通过</li></ul><hr><h3 id="4-OUTPUT"><a href="#4-OUTPUT" class="headerlink" title="4. OUTPUT"></a>4. OUTPUT</h3><p>数据包是<strong>本机发出去的</strong>。</p><p>比如：</p><ul><li>服务器主动 curl 某网站</li><li>本机发起 DNS 请求</li></ul><p>适合做：</p><ul><li>控制本机程序对外访问</li></ul><hr><h3 id="5-POSTROUTING"><a href="#5-POSTROUTING" class="headerlink" title="5. POSTROUTING"></a>5. POSTROUTING</h3><p>数据包<strong>已经决定了路由，即将发出网卡前</strong>。</p><p>适合做：</p><ul><li>SNAT</li><li>MASQUERADE</li></ul>]]></content>
    
    
    <categories>
      
      <category>技术原理</category>
      
      <category>Linux网络</category>
      
    </categories>
    
    
  </entry>
  
  
  
  <entry>
    <title>epoll事件触发方式</title>
    <link href="/2026/03/06/epoll%E4%BA%8B%E4%BB%B6%E8%A7%A6%E5%8F%91%E6%96%B9%E5%BC%8F/"/>
    <url>/2026/03/06/epoll%E4%BA%8B%E4%BB%B6%E8%A7%A6%E5%8F%91%E6%96%B9%E5%BC%8F/</url>
    
    <content type="html"><![CDATA[<p><code>LT</code> 和 <code>ET</code> 是 <code>epoll</code> 的两种事件触发模式：</p><ul><li><code>LT</code>：Level Trigger，水平触发</li><li><code>ET</code>：Edge Trigger，边缘触发</li></ul><p>可以把它们理解成：<strong>内核什么时候提醒你“这个 fd 可读&#x2F;可写了”</strong>。</p><p><strong>先说结论</strong></p><ul><li><code>LT</code>：只要 fd 还处于可读&#x2F;可写状态，就会一直通知你</li><li><code>ET</code>：只在状态“发生变化”的那一刻通知你一次</li></ul><hr><p><strong>LT：水平触发</strong></p><p>如果 socket 里还有数据没读完，那么它一直是“可读”的。</p><p>这时在 <code>LT</code> 模式下：</p><ul><li>你调用一次 <code>epoll_wait</code></li><li>内核告诉你：这个 fd 可读</li><li>你只读了一部分，没读完</li><li>下次 <code>epoll_wait</code>，内核还会继续告诉你：这个 fd 仍然可读</li></ul><p>也就是说：</p><ul><li><strong>条件没消失，就持续通知</strong></li><li>编程更直观，不容易漏事件</li></ul><p><strong>例子</strong></p><p>假设 socket 收到了 100 字节数据：</p><ul><li>你这次只读了 20 字节</li><li>还有 80 字节留在内核缓冲区</li><li>下次 <code>epoll_wait</code> 还会返回这个 fd 可读</li></ul><p>所以 LT 像是在说：</p><blockquote><p>“这个条件现在还成立，你还没处理完。”</p></blockquote><hr><p><strong>ET：边缘触发</strong></p><p><code>ET</code> 关注的是“状态变化的边缘”。</p><p>比如一个 socket 原来不可读，后来来了数据，变成可读：</p><ul><li>内核只会在这个“不可读 -&gt; 可读”的变化发生时通知你一次</li></ul><p>如果你收到通知后没有把数据一次性读完：</p><ul><li>缓冲区里虽然还留着数据</li><li>但因为“可读”这个状态没有再次变化</li><li>内核<strong>不一定再通知你</strong></li></ul><p>所以 ET 像是在说：</p><blockquote><p>“我提醒过你一次了，剩下你自己处理干净。”</p></blockquote><hr><p><strong>最关键的区别</strong></p><p>假设缓冲区里有数据，你没有读完：</p><ul><li><code>LT</code>：下次还会提醒你</li><li><code>ET</code>：下次可能不提醒你了</li></ul><p>所以 <code>ET</code> 模式下必须特别注意：</p><ul><li>一次事件来了后，要一直读</li><li>读到返回 <code>EAGAIN</code> &#x2F; <code>EWOULDBLOCK</code> 为止</li><li>写也是一样，要一直写到不能写为止</li></ul><hr><p><strong>为什么 ET 通常要配合非阻塞 fd</strong></p><p>因为在 ET 模式下你往往会这样写：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">while</span> ((n = read(fd, buf, <span class="hljs-keyword">sizeof</span>(buf))) &gt; <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-comment">// 处理数据</span><br>&#125;<br><span class="hljs-keyword">if</span> (n == <span class="hljs-number">-1</span> &amp;&amp; errno == EAGAIN) &#123;<br>    <span class="hljs-comment">// 说明已经读空了</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如果 fd 是阻塞的：</p><ul><li>你读着读着，数据没了</li><li><code>read</code> 可能会卡住</li><li>整个事件循环就被阻塞了</li></ul><p>所以实践里：</p><ul><li><code>ET</code> 基本都要搭配 <strong>非阻塞 socket</strong></li><li><code>LT</code> 虽然也常配非阻塞，但要求没那么死</li></ul><hr><p><strong>一个直观类比</strong></p><p>把 socket 缓冲区想成邮箱。</p><ul><li><code>LT</code>：只要邮箱里还有信，邮差每次都会提醒你“还有信”</li><li><code>ET</code>：只有“新信刚投进邮箱”的瞬间提醒你一次；你不把邮箱清空，下次未必再提醒</li></ul><hr><p><strong>LT 的特点</strong></p><ul><li>简单、稳妥</li><li>不容易漏读漏写</li><li>更适合新手和大多数普通场景</li><li>默认就是 LT</li></ul><p>如果你注册事件时不加 <code>EPOLLET</code>，那就是 LT。</p><hr><p><strong>ET 的特点</strong></p><ul><li>通知次数更少</li><li>在高并发场景下可能更高效</li><li>但代码更复杂，更容易出 bug</li><li>一旦没把缓冲区读空&#x2F;写尽，容易“丢通知”</li></ul><p>常见 bug：</p><ul><li>收到 <code>EPOLLIN</code> 后只读一次就结束</li><li>结果缓冲区还有数据</li><li>后面又等不到新的通知</li><li>连接看起来像“卡住了”</li></ul><hr><p><strong>代码上怎么区分</strong></p><p>LT：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">ev.events = EPOLLIN;<br></code></pre></td></tr></table></figure><p>ET：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">ev.events = EPOLLIN | EPOLLET;<br></code></pre></td></tr></table></figure><p>其中 <code>EPOLLET</code> 就表示边缘触发。</p><hr><p><strong>实际开发建议</strong></p><ul><li>如果你在学习、写普通网络程序：<strong>优先 LT</strong></li><li>如果你在做高性能服务器，并且非常清楚非阻塞 + 循环读写 + <code>EAGAIN</code> 的处理：可以用 <strong>ET</strong></li></ul><hr><p><strong>一句话总结</strong></p><ul><li><code>LT</code>：没处理完，内核会反复提醒</li><li><code>ET</code>：只在状态变化时提醒一次，你必须自己一次处理干净</li></ul>]]></content>
    
    
    <categories>
      
      <category>技术原理</category>
      
      <category>Linux网络</category>
      
    </categories>
    
    
  </entry>
  
  
  
  <entry>
    <title>弱网检测</title>
    <link href="/2026/02/28/%E5%BC%B1%E7%BD%91%E6%A3%80%E6%B5%8B/"/>
    <url>/2026/02/28/%E5%BC%B1%E7%BD%91%E6%A3%80%E6%B5%8B/</url>
    
    <content type="html"><![CDATA[<h2 id="项目架构"><a href="#项目架构" class="headerlink" title="项目架构"></a>项目架构</h2><p><img src="/pic/%E9%A1%B9%E7%9B%AE%E6%9E%B6%E6%9E%84.png"></p><h2 id="主体功能描述"><a href="#主体功能描述" class="headerlink" title="主体功能描述"></a>主体功能描述</h2><h2 id="具体功能实现"><a href="#具体功能实现" class="headerlink" title="具体功能实现"></a>具体功能实现</h2><h3 id="具备上网能力的网卡检测"><a href="#具备上网能力的网卡检测" class="headerlink" title="具备上网能力的网卡检测"></a>具备上网能力的网卡检测</h3><p>(<code>net_iface.h</code> &#x2F; <code>net_iface.cpp</code>) </p><p>这里负责检测系统中所有能连上互联网的网卡列表。</p><ul><li><p><strong>核心逻辑</strong>：通过使用netlink协议与内核进行通信，通过向内核发送 RTM_GETLINK 请求获取网卡信息，然后解析出处于Up状态的网卡名；通过向内核发送RTM_GETROUTE 请求获得路由信息，然后解析出具有默认网关的网卡的名字。 对动态列表检查，添加满足条件的网卡，同时删去不符合条件的。</p></li><li><p><strong>“具备上网能力”的判定标准（两者必须同时满足）</strong>：</p><ol><li><strong>状态为 UP 并排除回环网卡</strong>：监听链路状态确认是否存在 <code>IFF_UP</code> 标志位且不包含 <code>IFF_LOOPBACK</code>。</li><li><strong>拥有默认路由 (Default Route)</strong>：代码内部实现了 <code>isDefaultRoute()</code> 函数，检查是否有 IPv4 或 IPv6 的默认发向外部网关的路由（例如 <code>rtm_dst_len == 0</code>，表名为 <code>RT_TABLE_MAIN</code> 或 <code>RT_TABLE_DEFAULT</code> 等）。同时符合这两个条件的即可被判定为具备上网能力。</li></ol></li><li><p><strong>核心类名</strong>：<code>NetInterfaceManager</code>。</p></li></ul><h3 id="知识点"><a href="#知识点" class="headerlink" title="知识点"></a>知识点</h3><h4 id="网关是什么（Gateway-是什么）"><a href="#网关是什么（Gateway-是什么）" class="headerlink" title="网关是什么（Gateway 是什么）"></a>网关是什么（Gateway 是什么）</h4><p>在 IP 网络里，你的主机发一个包时会先判断目标 IP 是否“在本地网段”：</p><ul><li><strong>在本地网段</strong>：直接在二层（以太网&#x2F;Wi‑Fi）里 ARP&#x2F;ND 找到目标主机的 MAC，然后直发（不需要网关）。</li><li><strong>不在本地网段</strong>：主机没法二层直达目标，就需要把包交给一个能“继续转发”的设备 —— 这台设备就是<strong>网关&#x2F;路由器（gateway&#x2F;router）</strong>。</li></ul><p>所以“网关”可以理解为：<br><strong>“帮我把包送到别的网络去”的那台路由器的 IP（更准确说是 next-hop 的地址）。</strong></p><p>常见家庭网关例子：</p><ul><li>你电脑 IP：192.168.1.100&#x2F;24</li><li>路由器（网关）IP：192.168.1.1</li><li>你访问 8.8.8.8 这种公网地址时，会先把包发给 192.168.1.1</li></ul><hr><h4 id="外部网关是什么"><a href="#外部网关是什么" class="headerlink" title="外部网关是什么"></a>外部网关是什么</h4><p>“外部网关”不是一个特别严格的标准术语（不同团队&#x2F;文档会用得不一样），但在工程语境里通常指：</p><ul><li><strong>默认网关（default gateway）</strong>：把“去外网&#x2F;去未知网络”的流量发出去的那台网关</li><li>或者更口语一点：<strong>通往外部网络（比如互联网）的出口网关</strong></li></ul><p>“外部网关”基本等价于：</p><ul><li><strong>能把你这张网卡的流量带出本机所在网段、并作为默认出口的下一跳</strong><br>（注意：这个“外部”相对的是“本机&#x2F;本地链路”，不一定保证真能上互联网，只是“路由表上看起来有出口”。）</li></ul><p>也要注意一个常见误解：<br>有默认网关 ≠ 一定能上网</p><ul><li>可能网关不可达（ARP&#x2F;ND 不通）</li><li>可能 DNS 不通</li><li>可能被认证门户（酒店 Wi‑Fi）拦截</li><li>可能默认路由走的是 VPN&#x2F;容器&#x2F;策略路由等</li></ul><p>但**“存在默认路由&#x2F;默认网关”**确实是“看起来具备外网路径”的必要信号之一，因此很多程序会用它做初筛。</p><hr><h4 id="“IPv4-或-IPv6-的默认发向外部网关的路由”是什么"><a href="#“IPv4-或-IPv6-的默认发向外部网关的路由”是什么" class="headerlink" title="“IPv4 或 IPv6 的默认发向外部网关的路由”是什么"></a>“IPv4 或 IPv6 的默认发向外部网关的路由”是什么</h4><p>这句话可以拆成三层意思：</p><p><strong>A. 什么是“默认路由”（Default Route）</strong></p><p>默认路由就是“当没有更具体的路由规则匹配目标地址时，用这一条”。</p><ul><li><strong>IPv4 默认路由</strong>：目的网段是 0.0.0.0&#x2F;0</li><li><strong>IPv6 默认路由</strong>：目的网段是 ::&#x2F;0</li></ul><p>这俩的 &#x2F;0 表示“前缀长度为 0”，也就是<strong>匹配所有地址</strong>（最不具体，兜底用）。</p><p><strong>B. “发向外部网关”指什么</strong></p><p>默认路由通常长这样：</p><ul><li><p>IPv4（典型）：</p><p>default via 192.168.1.1 dev wlan0</p><ul><li>via 192.168.1.1 就是“下一跳网关”</li></ul></li><li><p>IPv6（典型）：</p><p>default via fe80::1 dev wlan0</p><ul><li>IPv6 的下一跳经常是 <strong>link-local</strong> 地址（fe80::&#x2F;10），来自路由器通告（RA）</li></ul></li></ul><p>也可能出现不写 via 的情况（比如 point-to-point、某些隧道或“on-link”默认路由），但工程里很多“判定默认网关”的逻辑主要就是找这种 default via … dev … &#x2F; 0.0.0.0&#x2F;0 via … &#x2F; ::&#x2F;0 via …。</p><p><strong>C. “IPv4 或 IPv6”是什么意思</strong></p><p>表示这台机器“对外的默认出口”可能是：</p><ul><li>只有 IPv4 默认路由（IPv4 能出网）</li><li>只有 IPv6 默认路由（IPv6 能出网）</li><li>两者都有（双栈）</li></ul><p>你的文里提到“检查是否有 IPv4 或 IPv6 的默认发向外部网关的路由”，通常就是在说：</p><ul><li>路由表里是否存在 0.0.0.0&#x2F;0 或 ::&#x2F;0</li><li>并且这条默认路由关联到某个网卡（出口 interface）</li><li>多数实现还会要求它是主路由表（Linux 下常见 RT_TABLE_MAIN 等）里的一条有效默认路由</li></ul><h4 id="AF-NETLINK"><a href="#AF-NETLINK" class="headerlink" title="AF_NETLINK"></a>AF_NETLINK</h4><p><code>AF_NETLINK</code> (Address Family - Netlink) 是 Linux 系统中一种专门用于<strong>内核态与用户态进程之间</strong>（或者用户态进程之间）进行双向通信的 <strong>Socket 协议族</strong>。</p><p>在传统的 Linux 编程中，我们要配置网络或者获取网卡状态，通常会使用 <code>ioctl</code> 系统调用，或者去读取 <code>/proc</code> 目录下的动态文件。但是 <code>AF_NETLINK</code> 被设计出来就是为了替代它们，作为一种更现代、更灵活、性能更高的网络状态交互机制。</p><p>简单来说，当你在代码里写下 <code>socket(AF_NETLINK, SOCK_RAW, ...)</code> 时，你就是在<strong>和 Linux操作系统的网络核心（协议栈）直接建立一个“聊天窗口”</strong>。</p><p><strong><code>AF_NETLINK</code> 的核心特点与优势：</strong></p><ol><li><p><strong>内核主动通知（双向通信）</strong>： 这是它最大的杀手锏。传统的 <code>ioctl</code> 只能由用户程序主动向内核发起查询（一问一答），如果你想知道网卡状态有没有改变，只能写个死循环不断去查（轮询），这极其消耗 CPU 性能。 而使用 <code>AF_NETLINK</code>，你可以向内核“订阅”某些事件。一旦发生了诸如“<strong>网线被拔掉</strong>”、“<strong>连接上了新的 Wi-Fi</strong>”、“<strong>路由表发生了变化</strong>”这样的事件，Linux 内核会主动通过这个 Socket 给你的程序发送一条消息通知。这也是为什么在这个弱网诊断项目中我们要用它——实现了纯<strong>事件驱动</strong>的网卡状态监听！</p></li><li><p><strong>支持多播（Multicast）</strong>： 内核产生的一个网络状态变化事件，可以通过多播组同时发送给系统里的多个监听程序（比如网络管家 NetworkManager 和你写的诊断程序可以同时收到网卡插拔通知）。你会发现你的代码里有 <code>addr.nl_groups = RTMGRP_LINK | RTMGRP_IPV4_ROUTE...</code>，这就是在订阅监听组。</p><ul><li><p><strong>单播（Unicast）</strong>：一对一，发给某一个具体主机（最常见）</p></li><li><p><strong>广播（Broadcast）</strong>：一对所有，发给同一二层网络里的所有主机（IPv4 有，IPv6 没有传统广播）</p></li><li><p><strong>多播（Multicast）</strong>：一对多，发给“加入某个组”的一批主机（比广播更“精准”）</p></li></ul></li><li><p><strong>标准 Socket 接口</strong>： 因为它是基于 Socket 的，所以你可以用标准的 <code>bind()</code>, <code>sendmsg()</code>, <code>recvmsg()</code> 来进行操作，而且非常方便和普通的网络通信一起挂载到 <code>epoll</code> 或 <code>select</code> 的事件循环中进行异步处理。</p></li></ol><h4 id="网卡状态"><a href="#网卡状态" class="headerlink" title="网卡状态"></a>网卡状态</h4><p><strong>核心的基础状态</strong></p><p>这些是判断网卡能不能用的最基本条件：</p><ul><li><code>IFF_UP</code> (启用状态)<ul><li><strong>含义</strong>：网卡接口已经被系统管理员或者系统底层配置工具激活（例如执行了 <code>ifconfig eth0 up</code> 或 <code>ip link set eth0 up</code>）。如果这个标志没有开启，网卡将彻底无法发送或接收数据，相当于被“软关闭”。</li><li><strong>在你们项目中的应用</strong>：无论网卡连不连得上外网，首先必须要拥有 <code>IFF_UP</code> 标志，才会被你的程序初步纳入考虑（见 <code>isUp</code> 的判断）。</li></ul></li><li><code>IFF_RUNNING</code> (运行&#x2F;就绪状态)<ul><li><strong>含义</strong>：这表示网卡资源已经分配完毕，通常也意味着<strong>物理链路</strong>可能已经建立。不过，在现代 Linux 系统中更推荐看下面的 <code>LOWER_UP</code>。</li></ul></li><li><code>IFF_LOWER_UP</code> (物理层连接状态)<ul><li><strong>含义</strong>：这是非常关键的一个标志！它代表<strong>网卡物理层（L1）已经连接成功</strong>。通俗来说，就是你的<strong>网线插好了而且另一端接通了路由器</strong>（绿灯亮了），或者是<strong>无线网卡已经成功关联上了一个 Wi-Fi 热点</strong>。</li><li><em>如果 <code>UP</code> 在但没有 <code>LOWER_UP</code>，说明系统启用了网卡，但没插线&#x2F;没连上WiFi。</em></li></ul></li><li><code>IFF_DORMANT</code> (休眠&#x2F;挂起状态)<ul><li><strong>含义</strong>：网卡是 UP 的，也有 LOWER_UP 物理连接，但是为了省电或者等待某些认证（比如以太网 802.1X 认证没通过前），它处于休眠挂起状态，此时不能正常收发普通数据包。</li></ul></li></ul><p><strong>网卡类型相关的状态</strong></p><p>这些标志描述了这张网卡是什么种类的网络设备：</p><ul><li><code>IFF_LOOPBACK</code> (回环接口)<ul><li><strong>含义</strong>：这是一个纯软件虚拟的接口，专门用来让本机自己和自己通信（通常分配的 IP 是 <code>127.0.0.1</code>）。</li></ul></li><li><code>IFF_POINTOPOINT</code> (点对点接口)<ul><li><strong>含义</strong>：这通常出现在 VPN（如 tun&#x2F;tap）设备、早期的串口拨号（PPP）网络或者 PPPoE 宽带拨号连接中。表示这个网卡的另一端只有唯一的对面节点，没有“同一个局域网上的其他邻居”。</li></ul></li></ul><p><strong>数据特征及工作模式相关的状态</strong></p><p>这些状态决定了网卡处理收发数据包的行为：</p><ul><li><code>IFF_BROADCAST</code> (支持广播)<ul><li>表明此网卡可以发送广播包给同一局域网下的所有人（以太网通常都有这个标志）。</li></ul></li><li><code>IFF_MULTICAST</code> (支持组播)<ul><li>表明该网卡支持多播&#x2F;组播。常见的 IPv6 邻居发现机制就依赖组播。</li></ul></li><li><code>IFF_PROMISC</code> (混杂模式)<ul><li><strong>含义</strong>：在这种模式下，网卡会抓取物理线上流经的<strong>所有数据包</strong>，即使这些数据包的收件人不是它自己。</li><li>这通常是为了做网络抓包（例如启动 Wireshark 抓包时，网卡就会进入这个状态）。</li></ul></li><li><code>IFF_ALLMULTI</code> (接收所有组播)<ul><li>接收网络上所有的组播数据包。</li></ul></li><li><code>IFF_NOARP</code> (无 ARP 协议)<ul><li><strong>含义</strong>：此接口不使用地址解析协议（ARP）去寻找目标 MAC 地址。纯 IP 点对点隧道（如 IPIP tunnel）或者某些特殊的虚拟网卡通常会有这个标志。</li></ul></li><li><code>IFF_DEBUG</code> (调试模式)<ul><li>用于打开内核中该网络接口的一些额外调试信息。</li></ul></li></ul><h4 id="RTM-GETLINK-获取链路-网卡信息"><a href="#RTM-GETLINK-获取链路-网卡信息" class="headerlink" title="RTM_GETLINK (获取链路&#x2F;网卡信息)"></a>RTM_GETLINK (获取链路&#x2F;网卡信息)</h4><ul><li>作用对象：针对的是系统的<strong>网络接口设备（Link&#x2F;Interface）本身</strong>，也就是网卡（比如 <code>eth0</code>, <code>wlan0</code></li><li>你可以获取到什么信息：<ul><li><strong>网卡序号 (ifindex)：</strong> 系统分配的唯一 ID（比如 1, 2, 3）。</li><li><strong>网卡名称 (ifname)：</strong> 比如 “eth0” 或 “wlp2s0”。</li><li><strong>网卡状态标志 (Flags)：</strong> 比如 <code>IFF_UP</code>（是否被系统启用）、<code>IFF_LOWER_UP</code>（网线是否插好&#x2F;WiFi是否连上）、<code>IFF_LOOPBACK</code>（是不是回环网卡）等。</li><li><strong>MTU、MAC 地址</strong> 等其他硬件属性。</li></ul></li></ul><h4 id="RTM-GETROUTE-获取路由表信息"><a href="#RTM-GETROUTE-获取路由表信息" class="headerlink" title="RTM_GETROUTE (获取路由表信息)"></a>RTM_GETROUTE (获取路由表信息)</h4><ul><li><strong>作用对象：</strong> 针对的是操作系统的<strong>路由表（Routing Table）</strong>。路由表就像是网络世界的高德地图，告诉系统：当你想访问某个 IP 地址时，应该把数据包交给哪张网卡发送。</li><li>你可以获取到什么信息：<ul><li><strong>目标网络域 (Destination)：</strong> 比如 <code>192.168.1.0/24</code>（局域网）或 <code>0.0.0.0/0</code>（代表外网&#x2F;互联网，也就是所谓的“默认路由”）。</li><li><strong>出口网卡 (OIF - Output Interface)：</strong> 这条路由规则最终要把包送到哪个网卡发出去（对应网卡的 <code>ifindex</code>）。</li><li><strong>网关地址 (Gateway)：</strong> 下一跳的路由器 IP 地址。</li><li>使用的确切 <strong>路由表 (Table ID)</strong> 和 <strong>优先级 (Metric)</strong>。</li></ul></li></ul><h4 id="DHCP"><a href="#DHCP" class="headerlink" title="DHCP"></a>DHCP</h4><p><strong>DHCP</strong> (Dynamic Host Configuration Protocol，<strong>动态主机配置协议</strong>) 是一个局域网的网络协议。它的主要作用是：<strong>自动给接入网络的设备（如电脑、手机、路由器等）分配 IP 地址以及其他网络配置。</strong></p><h4 id="使用linux命令获取路由表"><a href="#使用linux命令获取路由表" class="headerlink" title="使用linux命令获取路由表"></a>使用linux命令获取路由表</h4><ol><li><h5 id="现代最推荐：ip-route-或简写为-ip-r"><a href="#现代最推荐：ip-route-或简写为-ip-r" class="headerlink" title="现代最推荐：ip route (或简写为 ip r)"></a>现代最推荐：<code>ip route</code> (或简写为 <code>ip r</code>)</h5></li></ol><p>这是目前 Linux 中管理网络最强大也最标准的方法，属于 <code>iproute2</code> 工具包。<strong>你代码中 Netlink 接口获取的信息，和这个命令看到的是完全一致的</strong>，因为 <code>ip</code> 命令底层也是用 Netlink Socket 和内核通信的。</p><ol start="2"><li><h5 id="老派经典：route-n"><a href="#老派经典：route-n" class="headerlink" title="老派经典：route -n"></a>老派经典：<code>route -n</code></h5></li></ol><p>这是最传统的命令，属于 <code>net-tools</code> 工具包。虽然在新系统里被标为废弃，但因为输出格式像表格，很多人还是习惯用它。</p><h3 id="当前正在使用的主上网网卡检测"><a href="#当前正在使用的主上网网卡检测" class="headerlink" title="当前正在使用的主上网网卡检测"></a>当前正在使用的主上网网卡检测</h3><p> (<code>using_iface.h</code> &#x2F; <code>using_iface.cpp</code>)</p><p>如果系统有多张具备上网能力的网卡（比如同时连了Wi-Fi和插了网线），这段代码用于检测系统<strong>当前真实流量走的是哪一张</strong>。</p><h4 id="1-订阅-Linux-内核网络事件-Netlink-机制"><a href="#1-订阅-Linux-内核网络事件-Netlink-机制" class="headerlink" title="1. 订阅 Linux 内核网络事件 (Netlink 机制)"></a>1. 订阅 Linux 内核网络事件 (Netlink 机制)</h4><p>通过 <code>socket(AF_NETLINK, SOCK_RAW, NETLINK_ROUTE)</code> 建立一条与 Linux 内核直接通信的通道。 程序向内核订阅了 3 类主要事件：</p><ul><li><strong><code>RTMGRP_LINK</code></strong>：网卡链路状态变化（比如网线插拔、Wi-Fi 开启&#x2F;关闭、网卡被 UP&#x2F;DOWN）。</li><li><strong><code>RTMGRP_IPV4_ROUTE</code></strong>：IPv4 路由表的变化（比如 DHCP 获取到 IP 并下发了默认网关）。</li><li><strong><code>RTMGRP_IPV6_ROUTE</code></strong>：IPv6 路由表的变化。</li></ul><h4 id="2-核心状态筛选机制"><a href="#2-核心状态筛选机制" class="headerlink" title="2. 核心状态筛选机制"></a>2. 核心状态筛选机制</h4><p>程序在内存中维护了三个“集合（Set）”来追踪网卡状态：</p><ol><li><strong><code>upIfaces</code>（UP 状态集合）</strong>：过滤掉回环网卡（Loopback）后，只要网卡带有 <code>IFF_UP</code> 标志（代表网卡已启用），就将其索引（<code>ifindex</code>）加入此集合。</li><li><strong><code>v4Default</code>（IPv4 默认出口集合）</strong>：监听路由信息，如果一条路由是<strong>默认路由</strong>（目标为 <code>0.0.0.0/0</code>），且包含有效网关（Gateway），就将其绑定的出口网卡加入此集合。</li><li><strong><code>v6Default</code>（IPv6 默认出口集合）</strong>：逻辑同上，针对 <code>::/0</code> 的默认路由。</li></ol><ul><li><strong>解析封装</strong>：对内核主动下发的 <code>RTM_NEWLINK</code> &#x2F; <code>RTM_DELLINK</code> (网卡插拔、启用停用) 以及 <code>RTM_NEWROUTE</code> &#x2F; <code>RTM_DELROUTE</code> (路由的增加&#x2F;移除) 事件进行 <code>dispatch</code> 处理，最终挑选出当前优先级最高的主力网卡供功能模块后续使用。</li><li><strong>核心类名</strong>：<code>UsingInterfaceManager</code>。</li></ul><h4 id="3-生成“最终生效”的上网网卡"><a href="#3-生成“最终生效”的上网网卡" class="headerlink" title="3. 生成“最终生效”的上网网卡"></a>3. 生成“最终生效”的上网网卡</h4><p>当任意网卡状态或路由发生变化时，会触发状态发布（publishState 函数）。这里体现了系统的“仲裁逻辑”： 系统会去检查 <code>v4Default</code> 或 <code>v6Default</code> 集合里的网卡，<strong>并且要求该网卡必须同时也存在于 <code>upIfaces</code> (UP状态)集合中</strong>。</p><p>一旦找到同时满足：</p><ol><li><strong>是默认路由的出口层</strong></li><li><strong>物理层&#x2F;链路层处于 UP 状态</strong></li></ol><p>这个网卡就会被选定为 <strong>“当前正在使用的上网网卡”（Using Interface）</strong>。</p><h4 id="4-业务层的轮询与事件扩散"><a href="#4-业务层的轮询与事件扩散" class="headerlink" title="4. 业务层的轮询与事件扩散"></a>4. 业务层的轮询与事件扩散</h4><p>一旦选定这个主网卡，底层的 UsingInterfaceManager 首先会将网卡名称存入缓存。 上层的 server.cpp 有一个每 10 秒跑一次的监控线程：</p><ol><li>它通过调用weak_mgr-&gt;updateCurrentUsingSafe()去对比“上一次的上网网卡”和“最新的上网网卡”：<ul><li>如果发生了变化（比如用户断开了有线网络，系统路由自动切到了 Wi-Fi），它会立刻更新当前维护的网卡列表的 <code>usingNow</code> 布尔标志。</li></ul></li><li>然后，系统通过 DBus 信号（<code>emitChanged</code>）和事件管理器内部广播（<code>emitConnectionModeChanged</code>）通知所有的诊断模块（比如抓包工具、网速测试模块）：<strong>“主网卡变了，现在去监控新网卡的流量！”</strong>。</li></ol><h3 id="知识点-1"><a href="#知识点-1" class="headerlink" title="知识点"></a>知识点</h3><h4 id="RTMGRP-LINK（链路状态事件）"><a href="#RTMGRP-LINK（链路状态事件）" class="headerlink" title="RTMGRP_LINK（链路状态事件）"></a><code>RTMGRP_LINK</code>（链路状态事件）</h4><p>这一层处于网络的基础：主要关注网卡<strong>物理层和数据链路层</strong>的状态（可以将其理解为插网线或者开关 Wi-Fi 这种行为）。</p><p><strong>发生变化的原因（常见的场景）：</strong></p><ul><li><strong>插拔网线</strong>：网线被拔掉时，哪怕之前的 IP 还在、默认路由也没删掉，但链路肯定已经处于 <strong>DOWN</strong> 的状态了（<code>LOWER_DOWN</code>）。此时就不可能再上网。</li><li><strong>Wi-Fi 连接 &#x2F; 断开</strong>：笔记本连上一个新热点，或者因为信号不好掉线了。</li><li><strong>系统挂起 &#x2F; 唤醒</strong>：电脑休眠再唤醒，所有的网卡都会重新初始化。</li><li><strong>用户执行命令关闭 &#x2F; 开启了网卡</strong>：比如系统管理员在终端中执行了 <code>ip link set ens33 down</code> 命令。</li></ul><h4 id="RTMGRP-IPV4-ROUTE（IPv4-路由表事件）"><a href="#RTMGRP-IPV4-ROUTE（IPv4-路由表事件）" class="headerlink" title="RTMGRP_IPV4_ROUTE（IPv4 路由表事件）"></a><code>RTMGRP_IPV4_ROUTE</code>（IPv4 路由表事件）</h4><p>这一层关注“到达某个 IP 段从哪个网卡走”，也就是<strong>网络层的 IPv4 配置更新</strong>。对于我们寻找“当前上网口”来说，我们需要在大量的变化中捕捉一种特殊的路由条目：“<strong>IPv4 默认路由</strong>”（目的网段为 <code>0.0.0.0/0</code>）。</p><p><strong>发生变化的原因（常见的场景）：</strong></p><ul><li><strong>DHCP 获取 &#x2F; 更新了 IP 配置</strong>：当我们插上一根网线，或者连上 WiFi 后，网线那头的路由器不仅会分配我们一个本地 IP（比如 <code>192.168.1.5</code>），还会通过 DHCP 宣告**“我的 IP（网关）是 192.168.1.1，你们所有去公网的数据包都发给我”<strong>。这时操作系统会根据 DHCP 报文</strong>在系统的 IPv4 路由表里自动添加或者更新一条 0.0.0.0&#x2F;0 的规则**，从而引发一个 <code>RTM_NEWROUTE</code> 事件。</li><li><strong>VPN 拨号 &#x2F; 断开</strong>：当我们开启虚拟局域网或者代理时，这些客户端往往会在启动后<strong>把自己的虚拟网卡（例如 <code>tun0</code> ）强行设置为默认出口</strong>。此时，操作系统里的默认路由发生变更，系统会发来 <code>RTM_NEWROUTE</code> 且原有的默认路由可能被删掉 (<code>RTM_DELROUTE</code>)。当这件事情发生时，当前上网网卡就瞬间从原来的以太网&#x2F;Wi-Fi 切换到了这个虚拟网卡。</li><li><strong>管理员手动修改静态路由</strong>：比如有人执行了 <code>ip route add default via ...</code>。</li></ul><h4 id="RTMGRP-IPV6-ROUTE（IPv6-路由表事件）"><a href="#RTMGRP-IPV6-ROUTE（IPv6-路由表事件）" class="headerlink" title="RTMGRP_IPV6_ROUTE（IPv6 路由表事件）"></a><code>RTMGRP_IPV6_ROUTE</code>（IPv6 路由表事件）</h4><p>逻辑与上述一样，只是针对的是 IPv6 网络架构。需要关注的默认路由条目是 <code>::/0</code>。</p><p><strong>发生变化的原因（常见的场景）：</strong></p><ul><li><strong>SLAAC 路由器通告 (Router Advertisement, RA)</strong>：在 IPv6 环境下，很多时候不需要 DHCP。路由器每隔一段时间会<strong>多播发出它的路由通告 (RA)</strong>。如果它更新了配置导致了默认路线的变化，系统这里就会发生变化，而代码也会随着它进行处理放入 <code>v6Default</code> 集合。</li><li>手机开热点（如今多数智能手机热点提供纯 IPv6 或者 IPv4&#x2F;v6 双栈）。</li></ul><h4 id="0"><a href="#0" class="headerlink" title="::&#x2F;0"></a>::&#x2F;0</h4><p><code>::/0</code> 是 IPv6 网络中的<strong>默认路由（Default Route）</strong>，它在 IPv6 里的地位，完全等同于 IPv4 里的 <code>0.0.0.0/0</code>。</p><ol><li><p>:: 代表什么</p><p>在 IPv6 中，地址长达 128 位（写出来像这样：<code>fe80:0000:0000:0000:020c:29ff:fe4f:a27d</code>）。为了方便书写，IPv6 允许将连续的 <code>0</code> 简写为 :: （一个地址中只能出现一次）。 所以，<strong>单独的一个</strong> :: <strong>其实代表的就是 <code>0000:0000:0000:0000:0000:0000:0000:0000</code></strong>，即“全零地址”。</p></li><li><p><code>/0</code> 代表什么？</p></li></ol><p>   这代表<strong>子网掩码（前缀长度）为 0 位</strong>。 在路由匹配规则中：</p><ul><li>如果你写 <code>/64</code>，意思是要求目标 IP 的前 64 位必须和规则一致才算匹配。</li><li>如果你写 <code>/128</code>，意思是必须 128 位全匹配（也就是精确匹配一台特定的主机）。</li><li>当你写 <strong><code>/0</code></strong> 时，意思是**“只需匹配数据包目标 IP 的前 0 位”**——换句话说，不需要任何匹配条件，<strong>任何 IP 地址都能匹配上这条规则</strong>。</li></ul><h3 id="网卡状态收集与深入检测综合体"><a href="#网卡状态收集与深入检测综合体" class="headerlink" title="网卡状态收集与深入检测综合体"></a>网卡状态收集与深入检测综合体</h3><p> (<code>weak_netmgr.hpp</code> &#x2F; <code>weak_netmgr.cpp</code>)</p><p>这是一个综合的管理器，会把前面找到的“能力网卡”构建成自己的 <code>NetInfo</code> 列表，做更进一步的网络分析检测：</p><ul><li>在收到具体的上层网卡变化后，它会负责填充与管理当前网络更详细的状态矩阵（包含设置哪个网卡为当前主使用 <code>usingNow</code> 标志）。</li><li>包含了对无线网卡硬件能力的更细致处理，比如 WiFi 信号值（RSSI）更新等指标。</li><li><strong>核心类名</strong>：<code>WeakNetMgr</code>。</li></ul><h3 id="守护监控线程与事件触发"><a href="#守护监控线程与事件触发" class="headerlink" title="守护监控线程与事件触发"></a>守护监控线程与事件触发</h3><p>(<code>server.cpp</code> &#x2F; <code>event_manager.cpp</code>)</p><p>这里是将底层的检测能力进行周期性调度和消息广播分发的驱动器。</p><ul><li><strong>线程入口</strong>：在 <code>Server</code> 启动阶段，你会看到相关注释：<ul><li><code>// 启动网卡监控线程，实时维护具备上网能力的网卡列表</code></li><li><code>// 启动&quot;当前上网网卡&quot;监控线程</code></li></ul></li><li>如果这几个线程检测到了你的网卡（1. 由无法上网变为可以上网；或 2. 从 WiFi 自动切成了 4G&#x2F;有线 等主网卡改变时），会调用 <code>event_manager.cpp</code> 触发对应的 D-Bus 事件通知发送到客户端（例如 <code>InterfaceChanged</code> 信号）。</li></ul><h3 id="实时得到网卡的RTT"><a href="#实时得到网卡的RTT" class="headerlink" title="实时得到网卡的RTT"></a>实时得到网卡的RTT</h3><p>如果你看过常见的通过执行系统的 ping 命令去获取输出的做法，你会发现这个项目的写法并不一样。它的核心逻辑是一套高度定制化的 <strong>ICMP Raw Socket（原始套接字) + 强绑定网卡设备的探测机制</strong>：</p><h4 id="核心步骤一：创建原始套接字并“强绑定”网卡"><a href="#核心步骤一：创建原始套接字并“强绑定”网卡" class="headerlink" title="核心步骤一：创建原始套接字并“强绑定”网卡"></a>核心步骤一：创建原始套接字并“强绑定”网卡</h4><p>系统默认的 ping 只能走系统的默认路由，它是无法探测“非上网网卡”的延迟的。为了支持对所有能用的网卡进行同时探测，代码在 net_ping.cpp 中使用了 <code>SO_BINDTODEVICE</code> 套接字选项：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> sockfd = <span class="hljs-built_in">socket</span>(AF_INET, SOCK_RAW, IPPROTO_ICMP);<br><br><span class="hljs-comment">// Bind to device (超级核心：绑定到特定的网卡，如 ens33, wlan0 等)</span><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">ifreq</span> ifr&#123;&#125;;<br>std::<span class="hljs-built_in">snprintf</span>(ifr.ifr_name, IFNAMSIZ, <span class="hljs-string">&quot;%s&quot;</span>, ifaceName.<span class="hljs-built_in">c_str</span>());<br><span class="hljs-built_in">setsockopt</span>(sockfd, SOL_SOCKET, SO_BINDTODEVICE, &amp;ifr, <span class="hljs-built_in">sizeof</span>(ifr))<br></code></pre></td></tr></table></figure><p><strong>意义</strong>：这一步强制告诉 Linux 内核：“我接下来发出的这个 Ping 包，<strong>你不能查大路由表，必须硬生生从这个 <code>ifaceName</code> 的物理网卡口丢出去；接收回复时，也必须在这个口子上等。</strong>” 这就实现了对多张网卡并行单独检测的基础。</p><h4 id="核心步骤二：在-ICMP-载荷中”塞入”发送时间戳"><a href="#核心步骤二：在-ICMP-载荷中”塞入”发送时间戳" class="headerlink" title="核心步骤二：在 ICMP 载荷中”塞入”发送时间戳"></a>核心步骤二：在 ICMP 载荷中”塞入”发送时间戳</h4><p>为了精确计算 RTT，代码不需要依靠复杂的系统回溯。在构造 ICMP 报文 ( packIcmp 函数) 时：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">timeval</span>* tv = <span class="hljs-built_in">reinterpret_cast</span>&lt;<span class="hljs-keyword">struct</span> timeval*&gt;(icmp-&gt;icmp_data);<br><span class="hljs-built_in">gettimeofday</span>(tv, <span class="hljs-literal">nullptr</span>); <span class="hljs-comment">// 把当前秒/微秒时间放进里面</span><br></code></pre></td></tr></table></figure><p>它把数据包发送那一刻的系统时间（精确到微秒）当作行李，放到了 <code>ICMP ECHO</code> 数据包的载荷空间（Payload）里发了出去。</p><h4 id="核心步骤三：带有超时机制的等待-select"><a href="#核心步骤三：带有超时机制的等待-select" class="headerlink" title="核心步骤三：带有超时机制的等待 (select)"></a>核心步骤三：带有超时机制的等待 (<code>select</code>)</h4><p>发送数据包后，程序必须等待对方回信。为了防止死等，代码使用了 <code>select</code> 监听套接字：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> rv = <span class="hljs-built_in">select</span>(sockfd + <span class="hljs-number">1</span>, &amp;rfds, <span class="hljs-literal">nullptr</span>, <span class="hljs-literal">nullptr</span>, &amp;tv); <span class="hljs-comment">// tv里面塞入了 timeoutMs (默认 800ms)</span><br></code></pre></td></tr></table></figure><p>这就保证了程序的非阻塞性。如果过了 800ms 数据包仍没回来，<code>select</code> 就会超时退出，程序判定此次 ping 失败（认为严重丢包或者网络不通，返回负值如 -5 等）。</p><h4 id="核心步骤四：精确计算往返时间-RTT"><a href="#核心步骤四：精确计算往返时间-RTT" class="headerlink" title="核心步骤四：精确计算往返时间 (RTT)"></a>核心步骤四：精确计算往返时间 (RTT)</h4><p>一旦等到了 <code>recvfrom</code> 收到的对端服务器回复：</p><ol><li>先剥离外层的 IP 协议头，验证是不是自己刚刚发出去的那个包（核对 <code>icmp_id</code> 和 <code>ICMP_ECHOREPLY</code> 类型）。</li><li>因为 ICMP 协议标准规定，目标服务器回复 <code>ECHO REPLY</code> 时，<strong>必须把请求者当初发过去的 Payload 原封不动地带回来</strong>。</li><li>所以代码里只需再次获取系统当前的时间，减去包裹里原装的“发件时间”，即可精准计算出 RTT。</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">timeval</span>* sendTv = <span class="hljs-built_in">reinterpret_cast</span>&lt;<span class="hljs-keyword">struct</span> timeval*&gt;(ricmp-&gt;icmp_data);<br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">timeval</span> now&#123;&#125;;<br><span class="hljs-built_in">gettimeofday</span>(&amp;now, <span class="hljs-literal">nullptr</span>);<br><span class="hljs-comment">// 当前时间 - 里面携带的过去时间 = 往返总耗时 RTT</span><br><span class="hljs-type">long</span> rttMs = (now.tv_sec - sendTv-&gt;tv_sec) * <span class="hljs-number">1000</span> + (now.tv_usec - sendTv-&gt;tv_usec) / <span class="hljs-number">1000</span>;<br></code></pre></td></tr></table></figure><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>区别于简单的 <code>system(&quot;ping -c 1...&quot;)</code> 解析字符串方案，这套机制之所以好，是因为它直接在 C&#x2F;C++ 内存通过内核 Socket 发送，没有任何外部进程 fork 和字符串切片解析的开销，不仅能毫秒级精确拿捏 RTT 定时更新，还能通过绕过系统路由表的方式，测出所有非活跃网卡的延迟状态（比如连着两个 Wi-Fi 时，同时测出它们的延迟，为网卡切换做决策基础）。</p><h3 id="对wifi网卡信号强度的获取"><a href="#对wifi网卡信号强度的获取" class="headerlink" title="对wifi网卡信号强度的获取"></a>对wifi网卡信号强度的获取</h3><h4 id="核心逻辑"><a href="#核心逻辑" class="headerlink" title="核心逻辑"></a>核心逻辑</h4><p>在 Linux 下，网卡底层的无线状态（比如信号强度、连接密码、握手状态）是由一个叫做 <strong><code>wpa_supplicant</code></strong> 的系统守护进程统一接管的。 该项目并没有像一些简单的脚本那样去执行 <code>iwconfig</code> 或是读 &#x2F;proc&#x2F;net&#x2F;wireless 的文本文件，而是<strong>通过 Unix Domain Socket (UDS) 直接与系统级的 <code>wpa_supplicant</code> 进程进行对话交互</strong>，从而拿到最实时的底层信号评估。</p><p>主要流程可以分解为以下三步：</p><h4 id="1-创建本地-Unix-套接字并绑定-bind"><a href="#1-创建本地-Unix-套接字并绑定-bind" class="headerlink" title="1. 创建本地 Unix 套接字并绑定 (bind)"></a>1. 创建本地 Unix 套接字并绑定 (bind)</h4><p>为了能够和系统的系统服务对话，代码会先在本地（通常是 <code>/tmp</code> 目录）创建一个类型为 <code>AF_UNIX</code> 的数据报（<code>SOCK_DGRAM</code>）套接字，名字类似于 <code>/tmp/wpa_ctrl_&lt;进程pid&gt;_wlan0</code>。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">sockfd_ = ::<span class="hljs-built_in">socket</span>(AF_UNIX, SOCK_DGRAM, <span class="hljs-number">0</span>);<br>std::<span class="hljs-built_in">snprintf</span>(tmp, <span class="hljs-built_in">sizeof</span>(tmp), <span class="hljs-string">&quot;/tmp/wpa_ctrl_%d_%s&quot;</span>, <span class="hljs-built_in">getpid</span>(), iface_.<span class="hljs-built_in">c_str</span>());<br>::<span class="hljs-built_in">bind</span>(sockfd_, ...);<br></code></pre></td></tr></table></figure><h4 id="2-定位并连接到-wpa-supplicant-的控制端口"><a href="#2-定位并连接到-wpa-supplicant-的控制端口" class="headerlink" title="2. 定位并连接到 wpa_supplicant 的控制端口"></a>2. 定位并连接到 <code>wpa_supplicant</code> 的控制端口</h4><p><code>wpa_supplicant</code> 启动时，会在系统固定目录（最常见的是 <code>/var/run/wpa_supplicant</code> 或者 <code>/run/wpa_supplicant</code>）下，为每一个 Wi-Fi 网卡创建一个同名的 Socket 文件（比如 <code>/run/wpa_supplicant/wlan0</code>）。 代码里会遍历这些可能的目录，然后使用 <code>connect()</code> 连接过去。如果发现系统压根没起这个服务，代码甚至会<strong>尝试自己带上参数自动拉起一个 <code>wpa_supplicant</code> 进程</strong>（<code>launchWpaSupplicant</code> 函数）。</p><h4 id="3-发送特定指令拿到回包并解析-RSSI"><a href="#3-发送特定指令拿到回包并解析-RSSI" class="headerlink" title="3. 发送特定指令拿到回包并解析 (RSSI)"></a>3. 发送特定指令拿到回包并解析 (RSSI)</h4><p>这是最核心的交互。一旦连上系统的无线服务后，就调用 <code>sendCommand</code> 向守护进程发送一串字符指令：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">std::string resp = <span class="hljs-built_in">sendCommand</span>(<span class="hljs-string">&quot;SIGNAL_POLL\n&quot;</span>);<br></code></pre></td></tr></table></figure><p><strong><code>SIGNAL_POLL</code></strong> 是 <code>wpa_supplicant</code> 内置的一条查询指令（可以理解为对底层网卡驱动下达了一次雷达刷新指令），它会返回像下面这样的一行或多行结果：</p><blockquote><p>RSSI&#x3D;-42 LINKSPEED&#x3D;866 NOISE&#x3D;-92 FREQUENCY&#x3D;5180</p></blockquote><p>随后，代码只需对返回的字符串做简单的文本匹配找 <code>RSSI=</code>，即可把信号强度的 DBm（分贝毫瓦）绝对值（例如 <code>-42</code>）提取出来：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">size_t</span> pos = resp.<span class="hljs-built_in">find</span>(<span class="hljs-string">&quot;RSSI=&quot;</span>);<br><span class="hljs-keyword">if</span> (pos != std::string::npos) &#123;<br>    <span class="hljs-type">int</span> rssi = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">if</span> (std::<span class="hljs-built_in">sscanf</span>(resp.<span class="hljs-built_in">c_str</span>() + pos, <span class="hljs-string">&quot;RSSI=%d&quot;</span>, &amp;rssi) == <span class="hljs-number">1</span>) &#123;<br>        <span class="hljs-keyword">return</span> rssi;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h4><p>项目这种写法的目的在于：</p><ol><li><strong>低性能损耗</strong>：走 Socket IPC 进程间通信比在后台拉起 <code>iwconfig</code> 等子进程查询要高效得多。</li><li><strong>数据全面且实时</strong>：<code>SIGNAL_POLL</code> 是非常底层的驱动提取指令，它还能在未来被轻易扩展以获取信噪比（NOISE）、协商速率（LINKSPEED）甚至是频率，比翻阅那些需要一定时间才刷新的系统文件精确得多。</li></ol><h3 id="TCP丢包率检测"><a href="#TCP丢包率检测" class="headerlink" title="TCP丢包率检测"></a>TCP丢包率检测</h3><p>获取“丢包率”最传统的做法是 ping（ICMP 丢包率）或者抓包工具分析重传（例如 <code>tcpdump</code> 或 <code>tshark</code>)。但这两种方法在工程实现上有很大缺陷：Ping 只能反映 ICMP 线路质量，很多路由器甚至会丢弃 ICMP，不能真实反映 TCP 业务流的情况；而抓包方案开销太大，且无法长期挂在后台监控全局。</p><p><strong>这个项目使用了一种非常硬核且高效的在 Linux 内核原生拿数据的方案：<code>sock_diag / inet_diag</code> 机制。</strong></p><p>核心逻辑分为三个步骤：</p><h4 id="步骤一：通过-NETLINK-SOCK-DIAG-接口向内核下发“盘点”指令"><a href="#步骤一：通过-NETLINK-SOCK-DIAG-接口向内核下发“盘点”指令" class="headerlink" title="步骤一：通过 NETLINK_SOCK_DIAG 接口向内核下发“盘点”指令"></a>步骤一：通过 <code>NETLINK_SOCK_DIAG</code> 接口向内核下发“盘点”指令</h4><p>在 diagDumpFamilyIface 中，程序向内核发了一条类型为 <code>SOCK_DIAG_BY_FAMILY</code> 的 Netlink 消息：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs c++">msg.req.sdiag_family = <span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">uint8_t</span>&gt;(family);  <span class="hljs-comment">// IPv4 or IPv6</span><br>msg.req.sdiag_protocol = IPPROTO_TCP;                <span class="hljs-comment">// Chỉ看 TCP 协议</span><br>msg.req.idiag_states = <span class="hljs-number">0xFFFFFFFFu</span>;                  <span class="hljs-comment">// 所有状态都要拿</span><br>msg.req.idiag_ext = (<span class="hljs-number">1</span> &lt;&lt; (INET_DIAG_INFO - <span class="hljs-number">1</span>));     <span class="hljs-comment">// 要求附加详细信息(tcp_info)</span><br><br></code></pre></td></tr></table></figure><p>这就相当于对内核说：“立刻把系统底层此刻所有的 IPv4 和 IPv6 的 TCP 套接字状态，连同详细统计（<code>tcp_info</code> 结构），打包给我。”</p><h4 id="步骤二：遍历统计并过滤指定网卡-基于-Socket-ID-寻找归属"><a href="#步骤二：遍历统计并过滤指定网卡-基于-Socket-ID-寻找归属" class="headerlink" title="步骤二：遍历统计并过滤指定网卡 (基于 Socket ID 寻找归属)"></a>步骤二：遍历统计并过滤指定网卡 (基于 Socket ID 寻找归属)</h4><p>在内核把一堆 <code>inet_diag_msg</code> 信息吐回给程序后，它执行以下逻辑：</p><ol><li><strong>精准定位网卡</strong>：如果程序只想统计 <code>wlan0</code> 的丢包情况，它会在循环中用套接字信息自带的接口索引 <code>im-&gt;id.idiag_if</code> 去比对 <code>wlan0</code> 的索引号（把无关的流量排除）。</li><li><strong>提取并累加 <code>tcp_info</code> 指标</strong>：这是最核心的一步：</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp">tcp_info* ti = <span class="hljs-built_in">reinterpret_cast</span>&lt;tcp_info*&gt;(<span class="hljs-built_in">RTA_DATA</span>(attr));<br>totalRetrans += ti-&gt;tcpi_total_retrans; <span class="hljs-comment">// 分子：累加总重传数</span><br><br><span class="hljs-comment">// 分母近似：未确认的 + 已重传的 + 已收到的SACK的</span><br>segsOutApprox += <span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">uint64_t</span>&gt;(ti-&gt;tcpi_unacked)<br>               + <span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">uint64_t</span>&gt;(ti-&gt;tcpi_retrans)<br>               + <span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">uint64_t</span>&gt;(ti-&gt;tcpi_sacked);<br><br></code></pre></td></tr></table></figure><ul><li><strong><code>totalRetrans</code> (分子)</strong> 很容易理解，就是在这张网卡上走出去的，但是走中途丢了被迫重发的 TCP 段数。</li><li><strong><code>segsOutApprox</code> (分母)</strong>：由于部分 Linux 内核版本不提供宏观的 <code>tcpi_segs_out</code> 总发送段数指标，代码使用了一个非常巧妙的等效等式：<strong>已发出去还没收到回执的包 + 重传了的包 + 乱序了但确定送达的SACK包</strong>，利用这三者之和，近乎完美地还原出了真实的“<strong>尝试外出数据包</strong>（Segments Out）”当做计算的分母。如果这部分算出来实在没数据（分母为0），作为兜底政策，它会通过 <code>IFLA_STATS</code> 去获取这块物理网卡驱动记录的 <code>tx_packets</code> (发送的总帧数) 充当分母。</li></ul><h4 id="步骤三：滚动计算区间变化率（compute函数）"><a href="#步骤三：滚动计算区间变化率（compute函数）" class="headerlink" title="步骤三：滚动计算区间变化率（compute函数）"></a>步骤三：滚动计算区间变化率（compute函数）</h4><p>单次拿到的指标没用，因为内核计的是开机至今的累计重传。所以在上层的 tcp_loss_monitor.cpp 和 compute函数中，维护了前一次采样值：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">uint64_t</span> deltaOut = curr.outSegs - prev.outSegs;          <span class="hljs-comment">// 过去10秒一共新发了多少个TCP段</span><br><span class="hljs-type">uint64_t</span> deltaRetrans = curr.retransSegs - prev.retransSegs; <span class="hljs-comment">// 过去10秒这批包重传了多少个</span><br><span class="hljs-comment">// 计算并给出评级</span><br>r.ratePercent = (deltaRetrans * <span class="hljs-number">100.0</span>) / (<span class="hljs-type">double</span>)deltaOut;<br><br></code></pre></td></tr></table></figure><p>为了防止网络闲置状态时分母太小引发失真（例如发了2个包丢了1个，算出丢包率50%就过于夸张），系统默认限制了如果在检测间隔内发送的数据包总数没到一定阈值（比如这句 <code>if (result.sentDelta &gt;= 10)</code> ），就干脆不计算丢包率。</p><h4 id="为什么不用真实TCP发包数"><a href="#为什么不用真实TCP发包数" class="headerlink" title="为什么不用真实TCP发包数"></a>为什么不用真实TCP发包数</h4><p>主要是因为这个项目要的是：</p><ul><li><strong>按网卡</strong></li><li><strong>低开销</strong></li><li><strong>后台周期采样</strong></li><li><strong>不抓包也尽量能算</strong></li></ul><p>而“真实 TCP 发包数”在这个场景里<strong>很难直接、稳定、低成本地拿到</strong>。</p><p><strong>为什么不用“真实 TCP 发包数”</strong></p><ul><li>server&#x2F;src&#x2F;net_tcp.cpp:211 这条链路是按<strong>接口</strong>采样，不是按单个连接采样。</li><li>Linux 比较容易拿到的是：<ul><li><strong>全局 TCP 计数</strong>，比如 &#x2F;proc&#x2F;net&#x2F;snmp，但这是<strong>整台机器总量</strong>，不是某块网卡的，见 server&#x2F;include&#x2F;net_tcp.h:26 注释。</li><li><strong>每个 socket 的 tcp_info</strong>，可以通过 sock_diag 枚举，再按接口过滤，见 server&#x2F;src&#x2F;net_tcp.cpp:28。</li></ul></li><li>问题在于：项目当前走的是“<strong>每块网卡聚合所有 TCP socket</strong>”这条路，而这条路里并没有一个现成、统一、总能可靠使用的“该网卡真实 TCP 发段累计值”。</li></ul><p><strong>具体难点</strong></p><ul><li>全局计数不够细<ul><li>&#x2F;proc&#x2F;net&#x2F;snmp 里的 OutSegs、RetransSegs 是系统级的，不知道是 eth0 还是 wlan0 发的。</li></ul></li><li>网卡层计数不够准<ul><li>网卡有 tx_packets，但这是二层&#x2F;设备层发包数，不等于 TCP 段数。</li><li>它会混入 UDP、ARP、ICMP，甚至分片、重传、GSO&#x2F;TSO 等影响。</li></ul></li><li>抓包虽然更真，但代价高<ul><li>如果想要“真实 TCP 发包数”，最直接的是 pcap&#x2F;eBPF&#x2F;XDP&#x2F;TC 抓包统计。</li><li>但这会带来更高复杂度、权限要求和运行开销。</li><li>这个项目显然想做的是一个<strong>轻量后台监控器</strong>，不是流量探针。</li></ul></li><li>每 socket 的真实发送累计值也不一定好聚合<ul><li>理想上如果每个 socket 都能稳定拿到“累计发出段数”，再按 idiag_if 聚合就很好。</li><li>但当前实现作者显然认为这条路<strong>不够稳或不够通用</strong>，所以在 server&#x2F;src&#x2F;net_tcp.cpp:99 注释里直接写了“无法使用 tcpi_segs_out 时，采用近似估算”。</li></ul></li></ul><h4 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a>总结</h4><p>该机制<strong>不需要发送任何主动探测包 (免打扰)，不需要抓取分析网络流量 (极低 CPU 内存消耗)</strong>，它是在后台窃听了所有在本机上跑着的 APP (比如网页、视频会议流) 它们的 TCP 握手&#x2F;重传的底层行为，并将其汇聚转化为一个实时的整体丢包指标。</p><h3 id="知识点-2"><a href="#知识点-2" class="headerlink" title="知识点"></a>知识点</h3><h4 id="丢包率"><a href="#丢包率" class="headerlink" title="丢包率"></a>丢包率</h4><p><strong>丢包率（Packet Loss Rate）</strong>，顾名思义，就是指在网络传输过程中，<strong>丢失的数据包数量，占发送的数据包总数的百分比</strong>。</p><p>它是衡量网络连接质量最核心、最重要的指标之一（除了延迟&#x2F;RTT之外的另一大指标）。</p><h4 id="分母三个“指标”的具体含义"><a href="#分母三个“指标”的具体含义" class="headerlink" title="分母三个“指标”的具体含义"></a>分母三个“指标”的具体含义</h4><p><strong>① 已发出去还没收到回执的包 (<code>tcpi_unacked</code>)</strong></p><ul><li><strong>这是什么</strong>：这是 TCP 发送队列中的“生力军”。代表数据已经从你的网卡发往广域网了，正在半空中的光缆、交换机里传输（即 In-Flight），或者刚刚抵达了对方的服务器，但对方**还没有回传 ACK（确认收货）**的数据包集合。</li><li><strong>为什么算分母</strong>：只要对方没回 ACK，在发送端看来，这些包就依然处于“有可能会丢、需要我负责”的风险状态。</li></ul><p><strong>② 乱序了但确定送达的 SACK 包 (<code>tcpi_sacked</code>)</strong></p><ul><li><strong>这是什么</strong>：TCP 是要求严格按顺序抵达的。假设你发了 1, 2, 3, 4, 5 五个数据包结果 <strong>包2 半路丢了</strong>。对方收到了 1, 3, 4, 5。 在现代网络中，对方会利用 <strong>SACK（选择性确认）机制</strong>回复你：“喂，我收到 3,4,5 了，但 2 迟迟没来！” 此时，在你（发送端）的内核里，包 3、4、5 的状态就会从 <code>unacked</code> 变成 <code>sacked</code>。</li><li><strong>为什么算分母</strong>：虽然 3、4、5 实际上已经安全送达了，但因为“包2”丢了卡喉咙，这导致整个 TCP 分发流水线和滑动窗口<strong>无法向前推进清理库存</strong>。这三个包依然被迫停留在你的发送端窗口内等待“包2”重传补齐。如果不把它们加回来，当网络丢包严重（引发大量 SACK）时，你的分母就会变小，导致算出的丢包率严重失真暴涨。</li></ul><p><strong>③ 重传了的包 (<code>tcpi_retrans</code>)</strong></p><ul><li><strong>这是什么</strong>：当内核发现上面的“包2”确实丢了（比如等了很久触发了超时，或者收到了刚才的 SACK 警告），它就会紧急把“包2”再发一遍。这个刚刚发出去但<strong>依然没收到回执的“二手包2”</strong>，其状态就是 <code>tcpi_retrans</code>。</li><li><strong>为什么算分母</strong>：重传的包同样占用了你真实的上传带宽它也属于“此刻正在网络里飞行”的数据，因此必须计入总发包量中。</li></ul><p><strong>为什么要用它们相加作为分母？</strong></p><p>最完美的丢包率公式本来应该是：<strong>一段时间内新增的重传包数 &#x2F; 一段时间内新增的总发包数</strong>。</p><p>但是，在 Linux 系统的底层接口中，面临一个工程现实问题：</p><ol><li><code>tcpi_total_retrans</code>（累计重传包数）是一个<strong>只增不减</strong>的计数器，拿它做分母很好。</li><li>但在 Linux 早期的 <code>tcp_info</code> 结构定义中，<strong>并没有提供记录“开机至今总计发了多少包”的标量 (<code>tcpi_segs_out</code>)</strong>（这个字段是后来的高版本内核才加入的）。</li></ol><p>为了在不兼容的高低版本 Linux 系统里都能有一个可用的分母，这个项目的开发者采用了一种<strong>切片“快照”（Snapshot）推估法</strong>：</p><p>他们不去求“过去一共发了多少包”，而是去求**“此刻这一瞬间，系统的发送缓存里到底‘拖家带口’憋了多少个正在处理中的活跃数据包”**。也就是：</p><p><strong><code>近似总活跃发包量</code> &#x3D; <code>在路上正常跑的</code> + <code>虽然到了但卡住了的</code> + <code>正在补救重发的</code></strong></p><h4 id="什么是“滑动窗口（Sliding-Window）”？"><a href="#什么是“滑动窗口（Sliding-Window）”？" class="headerlink" title="什么是“滑动窗口（Sliding Window）”？"></a>什么是“滑动窗口（Sliding Window）”？</h4><p>你可以把数据传输想象成一个传送带上运送快递。发送方就是发货仓库，接收方就是收货仓库。</p><p>如果发货方发一件包裹，必须等收货方打个电话（发送 ACK 确认包）说“收到了”，然后再发下一件，那这个效率就太低了，这叫“停等协议”。</p><p>为了提高效率，TCP 引入了<strong>滑动窗口</strong>：</p><ol><li><strong>窗口的概念</strong>：发送方不需要等回复，可以<strong>一口气连续发出一批包裹</strong>。这“一批包裹”的最大数量，就是所谓的“窗口大小”。</li><li><strong>滑动的概念</strong>：由于接收方的仓库空间（接收缓存）是有限的，所以窗口不能无限大。当最早发出的几个包裹被确认安全送达后，这个“窗口”就可以<strong>向右滑动</strong>，空出名额，允许发送方继续发新的包裹。</li></ol><p><strong>它和代码中三个指标的关系：</strong></p><p>代码中写到的这三个状态包，<strong>正是被框在这个“滑动窗口”里面的所有包裹</strong>：</p><ul><li><strong><code>tcpi_unacked</code></strong>：窗口里已经扔上了传送带，但还没收到电话确认的那批包裹。</li><li><strong><code>tcpi_sacked</code></strong>：窗口里那些因为中间某个包裹丢了导致队伍卡住，虽然已经送达但被迫滞留在原地的包裹。</li><li><strong><code>tcpi_retrans</code></strong>：窗口里发现丢了，刚刚补发出去的那件包裹。</li></ul><p><strong>结论</strong>：<code>tcpi_unacked + tcpi_sacked + tcpi_retrans</code> 的总和，在物理意义上就等于<strong>当前这一瞬间，发送方滑动窗口中实际塞满的有效数据包总数（即 In-Flight 数据量）</strong>。用这个作为计算传输活跃度（分母），是非常符合 TCP 传输本质的。</p><h4 id="什么是“拥塞控制（Congestion-Control）”？"><a href="#什么是“拥塞控制（Congestion-Control）”？" class="headerlink" title="什么是“拥塞控制（Congestion Control）”？"></a>什么是“拥塞控制（Congestion Control）”？</h4><p>滑动窗口解决的是“收发双方”的处理能力匹配问题（流量控制），防止发送方把接收方淹没。 但如果发送方和接收方性能都极好，网络中间的路由器（比如小区宽带出口）受不了了怎么办？<strong>这就是拥塞控制要解决的网络整体堵车问题。</strong></p><p>它是发送方用来<strong>动态试探和调整发车速度</strong>的一套算法，主要包含以下几个阶段：</p><ol><li><strong>慢启动（Slow Start）</strong>：刚开始连接时，由于不知道马路上有多堵，先发 1 个包；顺利的话发 2 个；再顺利发 4 个、8 个…… 呈指数级增长。窗口迅速变大。</li><li><strong>拥塞避免（Congestion Avoidance）</strong>：加到一定程度（达到阈值 <code>ssthresh</code>），改用线性增长（每次加 1），一点一点试探网络的极限。</li><li><strong>拥塞发生（Congestion Occurs）</strong>：当网络终于不堪重负，<strong>出现了丢包</strong>。这时候，发送方就会立刻踩急刹车！</li></ol><p><strong>拥塞发生时的表现：</strong></p><p>当丢包发生时，TCP 是怎么发现的呢？正是通过我们前面提到的：</p><ul><li><strong>等待超时</strong>：一个包发出去很久（RTO），都没收到确认。此时 TCP 会认为网络严重堵塞，会将发包速度（拥塞窗口 <code>cwnd</code>）直接<strong>重置为 1</strong>，退回到最慢的慢启动状态，开始“重新做人”。</li><li><strong>收到三个重复的 ACK（触发快速重传）</strong>：接收方疯狂跟你说“我没收到包 2！”（这就是触发了 <code>tcpi_sacked</code>）。发送方意识到包 2 丢了，但还能收到后面的包，说明网络只是有点小卡，还能抢救。于是立即重发包 2（也就是产生了 <code>tcpi_retrans</code>），此时 TCP 会<strong>踩一脚缓刹车，把发送速度减半</strong>（快速恢复）。</li></ul><h4 id="为什么要通过这两种机制的数据来监控“弱网环境”？"><a href="#为什么要通过这两种机制的数据来监控“弱网环境”？" class="headerlink" title="为什么要通过这两种机制的数据来监控“弱网环境”？"></a>为什么要通过这两种机制的数据来监控“弱网环境”？</h4><ol><li><strong>宏观与微观的区别</strong>：应用层开发者（比如写 HTTP 业务、写网盘下载）只能看到“下载速度好像慢了”。但这就像是在岸上看江水变化，不够精准。</li><li><strong>内核是最敏锐的探针</strong>：因为有了滑动窗口和拥塞控制，<strong>Linux 系统内核的 TCP 协议栈是对整个网络健康度最敏感的器官</strong>。任何一次细微的波动、任何一个丢包，都会立即在它的滑动窗口里形成一个 <code>tcpi_retrans</code>（重传标记）或者一堆 <code>tcpi_sacked</code>，并触发拥塞控制去减速。</li></ol><p>代码的巧妙之处在于，它通过一条 Netlink 消息（<code>SOCK_DIAG</code>），绕过了笨重的应用层测试逻辑，<strong>直接“内窥（Diag）”了 Linux 内核正在运作的这个滑动窗口</strong>。</p><p>它看到了有多少包堵在了窗口里（分母），看到了有多少包被迫触发了快速重传或超时重传（分子），从而极速、无痛且极其精准地推算出了当前这根网线的“丢包率”，完美实现了**“诊断网络连接质量级别（Good &#x2F; Degraded &#x2F; Poor）”**的诊断目的。</p><h3 id="DBus设计"><a href="#DBus设计" class="headerlink" title="DBus设计"></a>DBus设计</h3><h4 id="标识体系"><a href="#标识体系" class="headerlink" title="标识体系"></a>标识体系</h4><table><thead><tr><th>标识</th><th>值</th></tr></thead><tbody><tr><td>Bus Name</td><td>com.example.WeakNet</td></tr><tr><td>Object Path</td><td>&#x2F;com&#x2F;example&#x2F;WeakNet</td></tr><tr><td>Interface</td><td>com.example.WeakNet</td></tr></tbody></table><h4 id="第一步：两个进程是完全独立的，内存不共享，怎么通信"><a href="#第一步：两个进程是完全独立的，内存不共享，怎么通信" class="headerlink" title="第一步：两个进程是完全独立的，内存不共享，怎么通信"></a>第一步：两个进程是完全独立的，内存不共享，怎么通信</h4><p>这个项目有两个独立的程序：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs arduino">服务端进程（weaknet-dbus-server）     客户端进程（test_client 或其他应用）<br>  — 持续监控网络状态                      — 想知道网络状态<br>  — 知道 RTT、RSSI、质量等级              — 想订阅网络变化通知<br></code></pre></td></tr></table></figure><p><strong>D-Bus 就是解决这个问题的中间人。</strong></p><hr><h4 id="第二步：D-Bus-守护进程是”快递中转站”"><a href="#第二步：D-Bus-守护进程是”快递中转站”" class="headerlink" title="第二步：D-Bus 守护进程是”快递中转站”"></a>第二步：D-Bus 守护进程是”快递中转站”</h4><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs">服务端进程  ──发消息──▶  dbus-daemon（系统内常驻）  ──转发──▶  客户端进程<br></code></pre></td></tr></table></figure><p>服务端和客户端都不直接联系对方，都只和 <code>dbus-daemon</code> 说话。</p><hr><h4 id="第三步：结合代码，看服务端怎么”入驻”"><a href="#第三步：结合代码，看服务端怎么”入驻”" class="headerlink" title="第三步：结合代码，看服务端怎么”入驻”"></a>第三步：结合代码，看服务端怎么”入驻”</h4><h5 id="3-1-连接到-D-Bus（server-cpp-第107-117行"><a href="#3-1-连接到-D-Bus（server-cpp-第107-117行" class="headerlink" title="3.1 连接到 D-Bus（server.cpp 第107-117行)"></a>3.1 连接到 D-Bus（server.cpp 第107-117行)</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 第一步：连接到 dbus-daemon（就像&quot;进公司上班打卡&quot;）</span><br>DBusConnection* conn = <span class="hljs-built_in">dbus_bus_get</span>(DBUS_BUS_SESSION, &amp;err);<br></code></pre></td></tr></table></figure><p>这一行之后，服务端进程就和 <code>dbus-daemon</code> 建立了连接，此时 D-Bus 给它分配了一个临时 ID，比如 <code>:1.42</code>。</p><h5 id="3-2-注册”门牌号”（server-cpp-第121行）"><a href="#3-2-注册”门牌号”（server-cpp-第121行）" class="headerlink" title="3.2 注册”门牌号”（server.cpp 第121行）"></a>3.2 注册”门牌号”（server.cpp 第121行）</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 第二步：申请一个固定的、有意义的名字（就像&quot;公司注册营业执照&quot;）</span><br><span class="hljs-built_in">dbus_bus_request_name</span>(conn, <span class="hljs-string">&quot;com.example.WeakNet&quot;</span>, <br>                      DBUS_NAME_FLAG_REPLACE_EXISTING, &amp;err);<br></code></pre></td></tr></table></figure><p>这一行之后，任何进程只要知道 <code>&quot;com.example.WeakNet&quot;</code> 这个名字，就能找到这个服务，<strong>不需要知道 PID</strong>。</p><h5 id="3-3-注册”服务窗口”（dbus-service-cpp-第44-48行）"><a href="#3-3-注册”服务窗口”（dbus-service-cpp-第44-48行）" class="headerlink" title="3.3 注册”服务窗口”（dbus_service.cpp 第44-48行）"></a>3.3 注册”服务窗口”（dbus_service.cpp 第44-48行）</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 第三步：告诉 D-Bus：&quot;发到 /com/example/WeakNet 这个地址的消息，</span><br><span class="hljs-comment">//         由我的 MessageHandlerStatic 函数来处理&quot;</span><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">DbusService::register_on_connection</span><span class="hljs-params">(DBusConnection* conn)</span> </span>&#123;<br>    <span class="hljs-type">static</span> DBusObjectPathVTable vtable&#123;&#125;;<br>    vtable.message_function = &amp;MessageHandlerStatic;  <span class="hljs-comment">// 处理函数</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">dbus_connection_register_object_path</span>(<br>        conn, <span class="hljs-string">&quot;/com/example/WeakNet&quot;</span>, &amp;vtable, <span class="hljs-keyword">this</span>);<br>&#125;<br></code></pre></td></tr></table></figure><hr><h4 id="第四步：客户端发”方法调用”，服务端处理并回复"><a href="#第四步：客户端发”方法调用”，服务端处理并回复" class="headerlink" title="第四步：客户端发”方法调用”，服务端处理并回复"></a>第四步：客户端发”方法调用”，服务端处理并回复</h4><p>这是<strong>请求-响应</strong>模式，就像打电话问问题：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">客户端：我要调用 com.example.WeakNet 的 Ping 方法，目标主机是 <span class="hljs-string">&quot;8.8.8.8&quot;</span><br>                ↓（通过 dbus-daemon 转发）<br>服务端：收到！<br></code></pre></td></tr></table></figure><h5 id="服务端的”总接线员”（dbus-service-cpp-第22-42行）"><a href="#服务端的”总接线员”（dbus-service-cpp-第22-42行）" class="headerlink" title="服务端的”总接线员”（dbus_service.cpp 第22-42行）"></a>服务端的”总接线员”（dbus_service.cpp 第22-42行）</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">static</span> DBusHandlerResult <span class="hljs-title">MessageHandlerStatic</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">        DBusConnection* conn, DBusMessage* msg, <span class="hljs-type">void</span>* user_data)</span> </span>&#123;<br>    <br>    <span class="hljs-keyword">auto</span>* self = <span class="hljs-built_in">reinterpret_cast</span>&lt;DbusService*&gt;(user_data); <span class="hljs-comment">// 找到服务对象</span><br><br>    <span class="hljs-comment">// 判断是哪个方法调用，分发给不同的处理函数</span><br>    <span class="hljs-keyword">if</span> (<span class="hljs-built_in">dbus_message_is_method_call</span>(msg, kInterface, <span class="hljs-string">&quot;Get&quot;</span>))<br>        &#123; self-&gt;<span class="hljs-built_in">handleGet</span>(conn, msg); <span class="hljs-keyword">return</span> DBUS_HANDLER_RESULT_HANDLED; &#125;<br><br>    <span class="hljs-keyword">if</span> (<span class="hljs-built_in">dbus_message_is_method_call</span>(msg, kInterface, <span class="hljs-string">&quot;ListInterfaces&quot;</span>))<br>        &#123; self-&gt;<span class="hljs-built_in">handleListInterfaces</span>(conn, msg); <span class="hljs-keyword">return</span> DBUS_HANDLER_RESULT_HANDLED; &#125;<br><br>    <span class="hljs-keyword">if</span> (<span class="hljs-built_in">dbus_message_is_method_call</span>(msg, kInterface, <span class="hljs-string">&quot;Ping&quot;</span>))<br>        &#123; self-&gt;<span class="hljs-built_in">handlePing</span>(conn, msg); <span class="hljs-keyword">return</span> DBUS_HANDLER_RESULT_HANDLED; &#125;<br><br>    <span class="hljs-keyword">return</span> DBUS_HANDLER_RESULT_NOT_YET_HANDLED; <span class="hljs-comment">// 不认识的消息，不处理</span><br>&#125;<br></code></pre></td></tr></table></figure><h5 id="处理-Ping-请求，发回复（dbus-service-cpp-第187行起"><a href="#处理-Ping-请求，发回复（dbus-service-cpp-第187行起" class="headerlink" title="处理 Ping 请求，发回复（dbus_service.cpp 第187行起)"></a>处理 Ping 请求，发回复（dbus_service.cpp 第187行起)</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">DbusService::handlePing</span><span class="hljs-params">(DBusConnection* conn, DBusMessage* msg)</span> </span>&#123;<br>    <br>    <span class="hljs-comment">// ① 从消息中取出客户端传来的参数 hostname</span><br>    <span class="hljs-type">const</span> <span class="hljs-type">char</span>* hostname = <span class="hljs-literal">nullptr</span>;<br>    <span class="hljs-built_in">dbus_message_get_args</span>(msg, &amp;err, DBUS_TYPE_STRING, &amp;hostname, DBUS_TYPE_INVALID);<br><br>    <span class="hljs-comment">// ② 找到当前正在用的网卡</span><br>    std::string currentIface;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">const</span> <span class="hljs-keyword">auto</span>&amp; net : ctx_-&gt;iface_list) &#123;<br>        <span class="hljs-keyword">if</span> (net.<span class="hljs-built_in">usingNow</span>()) &#123; currentIface = net.ifName(); <span class="hljs-keyword">break</span>; &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// ③ 真正执行 ping</span><br>    <span class="hljs-type">int</span> pingResult = NetPing::<span class="hljs-built_in">getInstance</span>()-&gt;<span class="hljs-built_in">ping</span>(hostname, currentIface, <span class="hljs-number">3000</span>);<br><br>    <span class="hljs-comment">// ④ 构建回复消息，发送给客户端</span><br>    DBusMessage* reply = <span class="hljs-built_in">dbus_message_new_method_return</span>(msg);<br>    std::string result = <span class="hljs-string">&quot;PING 8.8.8.8 via eth0: 12ms&quot;</span>; <span class="hljs-comment">// 示意</span><br>    <span class="hljs-built_in">dbus_message_iter_append_basic</span>(&amp;args, DBUS_TYPE_STRING, &amp;result);<br>    <span class="hljs-built_in">dbus_connection_send</span>(conn, reply, <span class="hljs-literal">nullptr</span>);  <span class="hljs-comment">// 回复客户端</span><br>    <span class="hljs-built_in">dbus_connection_flush</span>(conn);<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>整个流程图：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp">客户端                    dbus-daemon               服务端<br>  |                           |                        |<br>  |── <span class="hljs-built_in">Ping</span>(<span class="hljs-string">&quot;8.8.8.8&quot;</span>) ──────▶|── 转发 ──────────────▶ |<br>  |                           |                        | 执行真实 ping<br>  |                           |                        | 拿到结果 <span class="hljs-number">12</span>ms<br>  |◀── <span class="hljs-string">&quot;PING 8.8.8.8: 12ms&quot;</span> ─|◀─── 回复 ─────────────|<br></code></pre></td></tr></table></figure><hr><h4 id="第五步：服务端主动”广播”信号"><a href="#第五步：服务端主动”广播”信号" class="headerlink" title="第五步：服务端主动”广播”信号"></a>第五步：服务端主动”广播”信号</h4><p>这是<strong>发布-订阅</strong>模式，就像广播电台，不针对特定客户端：</p><p><strong>触发场景</strong>：网络质量变化了，服务端主动通知所有关心的客户端。</p><h5 id="信号的广播路径（server-cpp-第303-351行-→-dbus-service-cpp-第150行"><a href="#信号的广播路径（server-cpp-第303-351行-→-dbus-service-cpp-第150行" class="headerlink" title="信号的广播路径（server.cpp 第303-351行 → dbus_service.cpp 第150行)"></a>信号的广播路径（server.cpp 第303-351行 → dbus_service.cpp 第150行)</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp">network_quality_thread（后台线程，每<span class="hljs-number">15</span>秒检测一次）<br>  ↓ 发现质量从 <span class="hljs-string">&quot;good&quot;</span> 变成 <span class="hljs-string">&quot;poor&quot;</span><br><span class="hljs-built_in">getEventManager</span>().<span class="hljs-built_in">emitNetworkQualityChanged</span>(<span class="hljs-string">&quot;poor&quot;</span>, <span class="hljs-string">&quot;RTT=500ms&quot;</span>, <span class="hljs-string">&quot;assessor&quot;</span>)<br>  ↓（EventManager 是内部中转层）<br>DbusService::<span class="hljs-built_in">emitNetworkQualitySignal</span>(<span class="hljs-string">&quot;poor&quot;</span>, <span class="hljs-string">&quot;RTT=500ms&quot;</span>, counter)<br>  ↓ 构建信号消息并广播<br>dbus-daemon 收到信号<br>  ↓ 转发给所有订阅了这个信号的客户端<br>客户端的回调函数被触发<br></code></pre></td></tr></table></figure><h5 id="信号发送的实际代码（dbus-service-cpp-第150-184行"><a href="#信号发送的实际代码（dbus-service-cpp-第150-184行" class="headerlink" title="信号发送的实际代码（dbus_service.cpp 第150-184行)"></a>信号发送的实际代码（dbus_service.cpp 第150-184行)</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">DbusService::emitNetworkQualitySignal</span><span class="hljs-params">(...)</span> </span>&#123;<br>    <br>    <span class="hljs-comment">// ① 创建一个&quot;信号&quot;消息（不是方法调用，是广播）</span><br>    DBusMessage* signal = <span class="hljs-built_in">dbus_message_new_signal</span>(<br>        <span class="hljs-string">&quot;/com/example/WeakNet&quot;</span>,     <span class="hljs-comment">// 从哪个对象发出</span><br>        <span class="hljs-string">&quot;com.example.WeakNet&quot;</span>,      <span class="hljs-comment">// 接口名</span><br>        <span class="hljs-string">&quot;NetworkQualityChanged&quot;</span>     <span class="hljs-comment">// 信号名</span><br>    );<br><br>    <span class="hljs-comment">// ② 往信号里塞数据</span><br>    <span class="hljs-built_in">dbus_message_iter_append_basic</span>(&amp;iter, DBUS_TYPE_STRING, &amp;quality);  <span class="hljs-comment">// &quot;poor&quot;</span><br>    <span class="hljs-built_in">dbus_message_iter_append_basic</span>(&amp;iter, DBUS_TYPE_STRING, &amp;details);  <span class="hljs-comment">// &quot;RTT=500ms&quot;</span><br>    <span class="hljs-built_in">dbus_message_iter_append_basic</span>(&amp;iter, DBUS_TYPE_INT32,  &amp;counter);  <span class="hljs-comment">// 42</span><br><br>    <span class="hljs-comment">// ③ 广播出去（不需要指定接收者）</span><br>    <span class="hljs-built_in">dbus_connection_send</span>(ctx_-&gt;connection, signal, <span class="hljs-literal">nullptr</span>);<br>    <span class="hljs-built_in">dbus_connection_flush</span>(ctx_-&gt;connection);<br>&#125;<br></code></pre></td></tr></table></figure><hr><h4 id="第六步：主线程的-Looper-是”消息泵”"><a href="#第六步：主线程的-Looper-是”消息泵”" class="headerlink" title="第六步：主线程的 Looper 是”消息泵”"></a>第六步：主线程的 Looper 是”消息泵”</h4><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// server.cpp 最后几行</span><br><span class="hljs-keyword">auto</span>* lp = Looper::<span class="hljs-built_in">current</span>();<br>lp-&gt;<span class="hljs-built_in">attach</span>(ctx.connection); <span class="hljs-comment">// 绑定 D-Bus 连接</span><br>lp-&gt;<span class="hljs-built_in">run</span>(&amp;ctx);              <span class="hljs-comment">// 永远阻塞在这里，不断从 dbus-daemon 取消息并分发</span><br></code></pre></td></tr></table></figure><p>如果没有这个循环，服务端就无法收到任何来自客户端的方法调用请求。</p><h4 id="总结：两种通信模式"><a href="#总结：两种通信模式" class="headerlink" title="总结：两种通信模式"></a>总结：两种通信模式</h4><figure class="highlight scss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs scss">┌─────────────────────────────────────────────────────────────┐<br>│  模式一：方法调用（客户端问，服务端答）                         │<br>│                                                             │<br>│  客户端                              服务端                  │<br>│  <span class="hljs-built_in">weaknet_ping_host</span>(&quot;<span class="hljs-number">8.8</span>.<span class="hljs-number">8.8</span>&quot;)  ───▶  <span class="hljs-built_in">handlePing</span>()          │<br>│                                ◀───  &quot;<span class="hljs-number">12ms</span>&quot;                 │<br>└─────────────────────────────────────────────────────────────┘<br><br>┌─────────────────────────────────────────────────────────────┐<br>│  模式二：信号广播（服务端主动推，客户端被动收）                   │<br>│                                                             │<br>│  服务端后台线程检测到网络质量变化                               │<br>│      ↓                                                      │<br>│  <span class="hljs-built_in">emitNetworkQualitySignal</span>() 广播                            │<br>│      ↓           ↓          ↓                               │<br>│  客户端<span class="hljs-selector-tag">A</span>        客户端<span class="hljs-selector-tag">B</span>     客户端C  （所有订阅者都收到）        │<br>└─────────────────────────────────────────────────────────────┘<br></code></pre></td></tr></table></figure><p>两种模式组合在一起，就构成了这个项目完整的 D-Bus 通信设计。</p><h4 id="gRPC-vs-D-Bus"><a href="#gRPC-vs-D-Bus" class="headerlink" title="gRPC vs D-Bus"></a>gRPC vs D-Bus</h4><table><thead><tr><th>对比维度</th><th>D-Bus (Desktop Bus)</th><th>gRPC</th><th></th></tr></thead><tbody><tr><td><strong>主要应用场景</strong></td><td><strong>单机内</strong>的进程间通信（操作系统级别，系统组件、桌面应用）</td><td>**跨机器（网络）**的服务间通信（微服务架构、云原生、分布式系统）</td><td></td></tr><tr><td><strong>传输协议</strong></td><td>Unix Domain Sockets（同一台机器内存条级传输，不走网卡）</td><td>HTTP&#x2F;2 + TCP&#x2F;IP（跨网络传输）</td><td></td></tr><tr><td><strong>数据格式</strong></td><td>自己的基础数据结构</td><td>Protobuf（二进制压缩）</td><td></td></tr><tr><td><strong>通信模式</strong></td><td>请求-响应；<strong>非常擅长广播（Signal）</strong></td><td>单调调用、单向&#x2F;双向数据流</td><td></td></tr><tr><td><strong>性能&#x2F;量级</strong></td><td>极轻量，单机极快</td><td>解决高吞吐量网络传输的重型框架</td><td></td></tr></tbody></table>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
